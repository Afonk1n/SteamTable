# ЗАДАЧИ

## ✅ Выполненные задачи

1. ✅ Исправлен расчет дней смены тренда
   - Исправлена логика группировки дат: теперь используется уникальный список дат из Map (без дубликатов) - это гарантирует что ночь и день одной даты считаются как один день
   - Заменен `Math.ceil` на `Math.floor` для более точного расчета календарных дней (округление вниз вместо вверх)
   - Добавлено использование полдня (12:00:00) при создании объектов Date для избежания проблем с часовыми поясами при расчете разницы
   - Обновлены обе функции: `history_analyzeTrend()` и `history_updateTrends()`