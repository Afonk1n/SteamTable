# ЗАДАЧИ И ИДЕИ


## Текущие задачи

### 1. ✅ Реализация Telegram бота - ВЫПОЛНЕНО

**Статус**: Бот создан, ключ получен, Chat ID: 7145328462 ✅

**Цель**: Реализовать Telegram бота для уведомлений и мониторинга портфеля

**Выбранный вариант**: Через Google Apps Script (без отдельного сервера)

**Что сделано:**
- ✅ Создан файл `Telegram.gs` с полной реализацией функций
- ✅ Реализованы функции настройки и отправки сообщений
- ✅ Реализована проверка целевых цен с уведомлениями
- ✅ Реализован ежедневный отчет о портфеле
- ✅ Добавлены пункты меню для настройки и теста
- ✅ Интегрировано в процесс обновления цен (`unified_priceUpdate`)
- ✅ Интегрировано в ежедневный сброс (`invest_dailyReset`)

**Использование:**
1. Настроить бота: SteamTable → Telegram → Настроить Telegram
2. Ввести Bot Token и Chat ID (7145328462)
3. Протестировать: SteamTable → Telegram → Тест Telegram
4. Уведомления будут отправляться автоматически при обновлении цен

---

## Сценарии использования Telegram бота

### 🔔 Уведомления (автоматические)

#### 1. Уведомление при достижении целевой цены
- **Триггер**: После обновления цен в Invest
- **Условие**: `Текущая цена >= Цель` (для продажи)
- **Формат сообщения**:
  ```
  🎯 Цель достигнута!
  
  Предмет: [Название]
  Текущая цена: [цена] ₽
  Цель: [цена] ₽
  Прибыль: [прибыль] ₽ ([%]%)
  
  Рекомендация: ПРОДАТЬ
  ```

#### 2. Уведомление о сильной просадке (сигнал покупки)
- **Триггер**: После обновления цен в Invest
- **Условие**: `Текущая цена <= Цель * 0.5` (просадка 50%+)
- **Формат сообщения**:
  ```
  📉 Сильная просадка!
  
  Предмет: [Название]
  Текущая цена: [цена] ₽
  Цель: [цена] ₽
  Просадка: [%]%
  
  Рекомендация: КУПИТЬ
  ```

#### 3. Ежедневный отчет о портфеле
- **Триггер**: После завершения сбора цен (утром/вечером)
- **Условие**: После обновления PortfolioStats
- **Формат сообщения**:
  ```
  📊 Отчет по портфелю
  
  Общие вложения: [сумма] ₽
  Текущая стоимость: [сумма] ₽
  Прибыль/убыток: [сумма] ₽ ([%]%)
  
  Активных позиций: [количество]
  Прибыльных: [количество]
  Убыточных: [количество]
  ```

#### 4. Уведомление о важных изменениях
- **Триггер**: После обновления аналитики
- **Условия**:
  - Изменение рекомендации на "КУПИТЬ" или "ПРОДАТЬ"
  - Сильное изменение тренда (с падающего на растущий или наоборот)
  - Достижение новой фазы (например, переход на "ДНО")
- **Формат сообщения**:
  ```
  ⚠️ Важное изменение
  
  Предмет: [Название]
  Изменение: [описание изменения]
  Текущий тренд: [тренд]
  Фаза: [фаза]
  Рекомендация: [рекомендация]
  ```

---

### 📋 Команды бота (если реализуем интерактивность)

**Примечание**: Команды бота пока не реализуем (требуют webhook).

#### Команды (для будущего, не приоритетно):
- `/stats` - Текущая статистика портфеля
- `/top` - Топ-5 прибыльных/убыточных
- `/item [название]` - Информация о конкретном предмете
- `/alerts` - Настройка уведомлений (вкл/выкл)
- `/help` - Список команд

---

## Техническая реализация

### 1. Хранение конфигурации

**Использовать PropertiesService для безопасного хранения:**
```javascript
// Сохранение токена и Chat ID
function telegram_setConfig(botToken, chatId) {
  const props = PropertiesService.getScriptProperties()
  props.setProperty('telegram_bot_token', botToken)
  props.setProperty('telegram_chat_id', chatId)
}

// Получение конфигурации
function telegram_getConfig() {
  const props = PropertiesService.getScriptProperties()
  return {
    botToken: props.getProperty('telegram_bot_token'),
    chatId: props.getProperty('telegram_chat_id')
  }
}
```

### 2. Функция отправки сообщений

```javascript
function telegram_sendMessage(message, parseMode = 'HTML') {
  const config = telegram_getConfig()
  if (!config.botToken || !config.chatId) {
    console.error('Telegram: конфигурация не настроена')
    return { ok: false, error: 'not_configured' }
  }
  
  const url = `https://api.telegram.org/bot${config.botToken}/sendMessage`
  
  try {
    const response = UrlFetchApp.fetch(url, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      payload: JSON.stringify({
        chat_id: config.chatId,
        text: message,
        parse_mode: parseMode
      }),
      muteHttpExceptions: true
    })
    
    const result = JSON.parse(response.getContentText())
    
    if (result.ok) {
      return { ok: true }
    } else {
      console.error('Telegram API error:', result)
      return { ok: false, error: result.description || 'unknown' }
    }
  } catch (e) {
    console.error('Telegram send error:', e)
    return { ok: false, error: 'exception' }
  }
}
```

### 3. Проверка целевых цен

```javascript
function telegram_checkPriceTargets() {
  const investSheet = getInvestSheet_()
  if (!investSheet) return
  
  const lastRow = investSheet.getLastRow()
  if (lastRow <= 1) return
  
  // Читаем данные batch-запросом
  const count = lastRow - 1
  const names = investSheet.getRange(DATA_START_ROW, getColumnIndex(INVEST_COLUMNS.NAME), count, 1).getValues()
  const currentPrices = investSheet.getRange(DATA_START_ROW, getColumnIndex(INVEST_COLUMNS.CURRENT_PRICE), count, 1).getValues()
  const goals = investSheet.getRange(DATA_START_ROW, getColumnIndex(INVEST_COLUMNS.GOAL), count, 1).getValues()
  const profits = investSheet.getRange(DATA_START_ROW, getColumnIndex(INVEST_COLUMNS.PROFIT), count, 1).getValues()
  const profitPercents = investSheet.getRange(DATA_START_ROW, getColumnIndex(INVEST_COLUMNS.PROFIT_AFTER_FEE), count, 1).getValues()
  const recommendations = investSheet.getRange(DATA_START_ROW, getColumnIndex(INVEST_COLUMNS.RECOMMENDATION), count, 1).getValues()
  
  // Проверяем каждую позицию
  for (let i = 0; i < count; i++) {
    const name = String(names[i][0] || '').trim()
    if (!name) continue
    
    const currentPrice = Number(currentPrices[i][0]) || 0
    const goal = Number(goals[i][0]) || 0
    const profit = Number(profits[i][0]) || 0
    const profitPercent = Number(profitPercents[i][0]) || 0
    const recommendation = String(recommendations[i][0] || '').trim()
    
    if (goal <= 0 || currentPrice <= 0) continue
    
    // Проверка достижения цели
    if (currentPrice >= goal) {
      const message = `🎯 Цель достигнута!\n\n` +
        `Предмет: <b>${name}</b>\n` +
        `Текущая цена: ${currentPrice.toFixed(2)} ₽\n` +
        `Цель: ${goal.toFixed(2)} ₽\n` +
        `Прибыль: ${profit.toFixed(2)} ₽ (${(profitPercent * 100).toFixed(2)}%)\n\n` +
        `Рекомендация: ${recommendation}`
      
      telegram_sendMessage(message)
      Utilities.sleep(500) // Пауза между сообщениями
    }
    
    // Проверка сильной просадки
    if (currentPrice <= goal * 0.5) {
      const dropPercent = ((goal - currentPrice) / goal) * 100
      const message = `📉 Сильная просадка!\n\n` +
        `Предмет: <b>${name}</b>\n` +
        `Текущая цена: ${currentPrice.toFixed(2)} ₽\n` +
        `Цель: ${goal.toFixed(2)} ₽\n` +
        `Просадка: ${dropPercent.toFixed(2)}%\n\n` +
        `Рекомендация: КУПИТЬ`
      
      telegram_sendMessage(message)
      Utilities.sleep(500)
    }
  }
}
```

### 4. Отправка ежедневного отчета

```javascript
function telegram_sendDailyReport() {
  const statsSheet = getOrCreatePortfolioStatsSheet_()
  if (!statsSheet) return
  
  // Читаем данные из PortfolioStats
  const totalInvestment = statsSheet.getRange(4, 2).getValue() || 0
  const totalCurrentValue = statsSheet.getRange(5, 2).getValue() || 0
  const totalProfit = statsSheet.getRange(6, 2).getValue() || 0
  const totalProfitPercent = statsSheet.getRange(6, 3).getValue() || 0
  const totalPositions = statsSheet.getRange(8, 2).getValue() || 0
  
  // Читаем топ-3 прибыльных
  const top3Data = []
  for (let i = 0; i < 3; i++) {
    const row = 11 + i // Начало данных топ-5
    const name = statsSheet.getRange(row, 1).getValue()
    const profit = statsSheet.getRange(row, 2).getValue()
    if (name && profit) {
      top3Data.push({ name, profit })
    }
  }
  
  const message = `📊 Отчет по портфелю\n\n` +
    `Общие вложения: <b>${totalInvestment.toFixed(2)}</b> ₽\n` +
    `Текущая стоимость: <b>${totalCurrentValue.toFixed(2)}</b> ₽\n` +
    `Прибыль/убыток: <b>${totalProfit.toFixed(2)}</b> ₽ (${(totalProfitPercent * 100).toFixed(2)}%)\n\n` +
    `Активных позиций: <b>${totalPositions}</b>\n\n` +
    (top3Data.length > 0 ? `Топ-${top3Data.length} прибыльных:\n` + 
      top3Data.map((item, idx) => `${idx + 1}. ${item.name} - ${item.profit.toFixed(2)} ₽`).join('\n') : '')
  
  telegram_sendMessage(message)
}
```

### 5. Интеграция в существующие функции

**В `unified_priceUpdate` (после обновления Invest):**
```javascript
// После обновления Invest
try {
  telegram_checkPriceTargets()
} catch (e) {
  console.error('Telegram: ошибка проверки целей:', e)
}
```

**В `invest_dailyReset` (после обновления PortfolioStats):**
```javascript
// После обновления PortfolioStats
try {
  telegram_sendDailyReport()
} catch (e) {
  console.error('Telegram: ошибка отправки отчета:', e)
}
```

---

## План реализации

### Этап 1: Базовая настройка ✅
- [x] Создать бота через @BotFather
- [x] Получить Bot Token
- [x] Получить Chat ID: 7145328462
- [x] Создать функцию `telegram_setConfig()` для сохранения конфигурации
- [x] Создать функцию `telegram_getConfig()` для получения конфигурации

### Этап 2: Базовые функции ✅
- [x] Создать функцию `telegram_sendMessage()` для отправки сообщений
- [x] Создать тестовую функцию для проверки подключения
- [x] Добавить функцию в меню для настройки Telegram

### Этап 3: Уведомления ✅
- [x] Создать функцию `telegram_checkPriceTargets()` для проверки целей
- [x] Интегрировать проверку в `unified_priceUpdate`
- [x] Создать функцию `telegram_sendDailyReport()` для ежедневного отчета
- [x] Интегрировать отчет в `invest_dailyReset`

### Этап 4: Дополнительные уведомления (опционально)
- [ ] Уведомления о важных изменениях (тренд, фаза, рекомендация)
- [ ] Настройка частоты уведомлений (через PropertiesService)

---

## Файлы для создания

1. **Telegram.gs** - новый файл с функциями для работы с Telegram API
   - `telegram_setConfig(botToken, chatId)` - настройка
   - `telegram_getConfig()` - получение конфигурации
   - `telegram_sendMessage(message, parseMode)` - отправка сообщения
   - `telegram_checkPriceTargets()` - проверка целей
   - `telegram_sendDailyReport()` - ежедневный отчет
   - `telegram_testConnection()` - тест подключения

2. **Обновить Menu.gs**:
   - Добавить пункт меню "Настроить Telegram" → `telegram_setup()`
   - Добавить пункт "Тест Telegram" → `telegram_testConnection()`

---

## Безопасность

- ✅ Токен и Chat ID хранятся в `PropertiesService` (не видны в коде)
- ✅ Токен не логируется в консоль
- ⚠️ При ошибках отправки не раскрывать детали в сообщениях пользователю

---

## Ограничения

- Apps Script имеет лимит на количество запросов (20,000 в день)
- Максимальная длина сообщения Telegram: 4096 символов
- Нет webhook (нужно проверять условия через триггеры)
- Интерактивные команды требуют webhook (можно добавить позже)

---

## Идеи для будущего (не приоритетные)

### 3. История успешных сделок и анализ
- **Что**: Анализ проданных предметов из Sales для улучшения стратегии
- **Метрики**:
  - Средняя прибыльность проданных позиций
  - Среднее время удержания позиции
  - Какие фазы/тренды дали лучший результат
  - Процент успешных сделок
- **Реализация**: Новый лист "Analytics" с формулами, анализирующими Sales

**Комментарий пользователя**: "Интересно пока думаем"

---

### 4. Визуализация трендов
- **Что**: Автоматическое создание графиков для выбранных предметов
- **Варианты**:
  - График цены за период (используя встроенные графики Google Sheets)
  - График прибыльности портфеля
  - Сравнение нескольких предметов
- **Реализация**: Функция, создающая графики через `insertChart()` или рекомендации по ручному созданию

**Комментарий пользователя**: "Интересно, пока думаем"

---

### 5. Дополнительные метрики в History
- **Что**: Новые колонки с полезной аналитикой
- **Метрики**:
  - Волатильность (стандартное отклонение цен за период)
  - Средняя цена за 7/30 дней
  - Изменение цены за период (%)
  - Максимальная просадка от пика
- **Реализация**: Новые функции расчета в `History.gs`, новые колонки

**Комментарий пользователя**: "Пока думаем, нужны только реально применимые данные в такой экономике как стим"

---

### 6. Умные рекомендации на основе истории
- **Что**: Анализ прошлых успешных сделок для улучшения рекомендаций
- **Логика**:
  - Если предмет с похожими характеристиками (фаза, тренд, потенциал) дал прибыль в прошлом → повысить приоритет
  - Учет времени удержания позиции
  - Учет волатильности предмета
- **Реализация**: Машинное обучение (простое) или правила на основе истории Sales

**Комментарий пользователя**: "Интересно, пока думаем"

---

### 7. Оптимизация производительности

**Текущее состояние:**
- ✅ **Уже реализовано:**
  - Batch-операции для чтения/записи данных
  - Оптимизированы `history_updateTrends()`, `history_updateCurrentPriceMinMax_()`
  - Оптимизированы функции синхронизации - batch-чтение из History
  - Оптимизировано форматирование колонок дат

- ⚠️ **Потенциальные улучшения:**
  - **Кеширование**: Использовать `CacheService` для кеширования данных History (5-10 минут)
  - **Ленивая загрузка**: Загружать данные только для нужных предметов
  - **Условное форматирование**: Проверить, не создаются ли лишние правила

- 📊 **Оценка**: Текущая оптимизация уже значительно улучшила скорость. Основные узкие места - API-запросы к Steam Market (не оптимизируются).

---

## Удаленные идеи (не нужны или реализованы)

- ❌ Фильтры и умная сортировка - "Пока что нет"
- ❌ Экспорт данных - "пока нет"
- ❌ Автоматическое резервное копирование - "Пока нет"
- ❌ Расширенная аналитика по периодам - "Писал выше про разные показатели" (дублирует идею 5)
