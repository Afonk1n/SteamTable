# ЗАДАЧИ И ИДЕИ

1. Очистить выполненые задачи, актуализировать документацию
2. Прочитать комментарии, дать идеи

---

## Информация о диаграммах и графиках для PortfolioStats

**Статус**: ❌ Не требуется (пользователь будет создавать диаграммы вручную)

**Резюме**: Google Sheets поддерживает программное создание диаграмм через `insertChart()`, но рекомендуется создавать их вручную через интерфейс - это более гибкий подход. Данные в PortfolioStats уже структурированы и готовы для создания диаграмм.

---

## Триггеры Telegram

### Моменты срабатывания уведомлений Telegram:

#### 1. 🔔 Уведомление при достижении целевой цены (`telegram_checkPriceTargets`)

**Триггер**: Вызывается в функции `unified_priceUpdate()` после обновления цен в Invest

**Когда срабатывает:**
- После каждого обновления цен через `unified_priceUpdate()` (каждые 10 минут)
- После синхронизации цен из History в Invest
- **Условие**: `Текущая цена >= Цель` (для продажи)

**⚠️ ПРОБЛЕМА (выявлена пользователем):**
- `telegram_checkPriceTargets()` вызывается в `unified_priceUpdate()` каждый раз, даже когда сбор цен завершен
- Это может привести к спаму уведомлениями каждые 10 минут для одной и той же позиции
- `unified_priceUpdate()` продолжает работать даже после завершения сбора (пока не начнется новый период)

**💡 ПРЕДЛОЖЕНИЯ ПО УЛУЧШЕНИЮ:**

1. **Отдельный триггер для проверки цен:**
   - Создать отдельный триггер, который срабатывает реже (например, раз в час)
   - Вызывать `telegram_checkPriceTargets()` только из этого триггера
   - Убрать вызов из `unified_priceUpdate()`

2. **Механизм предотвращения спама:**
   - Сохранять в `PropertiesService` или отдельном листе информацию о последних отправленных уведомлениях
   - Формат: `{itemName: {lastNotificationTime, lastPrice, notificationType}}`
   - Не отправлять повторное уведомление, если:
     - Прошло менее N часов (например, 6 часов) с последнего уведомления
     - Цена не изменилась значительно (например, менее 5%)
     - Уведомление того же типа уже было отправлено

3. **Проверка только при реальном изменении:**
   - Сохранять предыдущие цены и сравнивать с текущими
   - Отправлять уведомление только если цена изменилась (например, на 1%+)
   - Это предотвратит спам при стабильной цене

4. **Умная логика уведомлений:**
   - Для "достижения цели": отправлять только один раз, когда цена впервые достигла цели
   - Для "просадки": отправлять только при ухудшении ситуации (цена упала еще ниже)
   - Добавить флаг "уведомление отправлено" в Invest (можно использовать скрытую колонку или PropertiesService)

**Файл**: `Utils.gs`, строка 378
```javascript
telegram_checkPriceTargets()
```

**Что проверяется:**
- Все позиции в Invest
- Для каждой позиции: `currentPrice >= goal`
- Если условие выполнено → отправляется уведомление

---

#### 2. 📉 Уведомление о сильной просадке (`telegram_checkPriceTargets`)

**Триггер**: Тот же, что и выше - `telegram_checkPriceTargets()`

**Когда срабатывает:**
- После каждого обновления цен через `unified_priceUpdate()` (каждые 10 минут)
- **Условие**: `Текущая цена <= Цель * 0.5` (просадка 50%+)

**Что проверяется:**
- Все позиции в Invest
- Для каждой позиции: `currentPrice <= goal * 0.5`
- Если условие выполнено → отправляется уведомление о просадке

---

#### 3. 📊 Ежедневный отчет о портфеле (`telegram_sendDailyReport`)

**Триггер**: Вызывается в функции `invest_dailyReset()` после обновления PortfolioStats

**Когда срабатывает:**
- После ежедневного сброса Invest (`invest_dailyReset()`)
- После обновления аналитики портфеля (`portfolioStats_update()`)
- **Время**: Обычно утром (после завершения сбора цен за ночь) или вечером (после завершения сбора цен за день)

**Файл**: `Invest.gs`, строка 142
```javascript
telegram_sendDailyReport()
```

**Что отправляется:**
- Общие вложения
- Текущая стоимость
- Прибыль/убыток (в рублях и %)
- Количество активных позиций
- Количество прибыльных/убыточных позиций

**⚠️ ПРОБЛЕМА (выявлена пользователем):**
- Данные мало информативны для долгих инвестиций
- Точечные изменения (2-3 позиции готовы к продаже) теряются в общей картине
- Нужна более детальная аналитика

**💡 ПРЕДЛОЖЕНИЯ ПО УЛУЧШЕНИЮ:**

1. **Добавить секцию "Готовы к продаже":**
   - Позиции, где `currentPrice >= goal` (достигли цели)
   - Позиции с рекомендацией "🟥 ПРОДАТЬ"
   - Позиции с высокой прибылью (>30%) и трендом "🟥 Падает"
   - Формат: "3 позиции готовы к продаже: [Название1] (+45%), [Название2] (+38%)..."

2. **Добавить секцию "Изменения за день/неделю":**
   - Новые позиции (добавлены сегодня/на этой неделе)
   - Позиции, достигшие цели (впервые за период)
   - Позиции с сильным изменением прибыльности (>10% за день)
   - Формат: "За сегодня: +2 новые позиции, 1 достигла цели, +5.2% к общей прибыли"

3. **Добавить секцию "Требуют внимания":**
   - Позиции с сильной просадкой (>20%)
   - Позиции с изменением тренда (с растущего на падающий)
   - Позиции с изменением фазы (например, переход на "🔥 ПИК")
   - Формат: "⚠️ Внимание: [Название] - просадка 25%, тренд изменился на падающий"

4. **Добавить аналитику по периодам:**
   - Сравнение с предыдущим днем/неделей
   - Изменение общей прибыльности
   - Изменение количества позиций
   - Формат: "📈 За неделю: +12.5% к прибыли, +3 позиции, -2 продано"

5. **Добавить детализацию по категориям:**
   - Разбивка по фазам: сколько на дне, сколько на пике
   - Разбивка по трендам: сколько растет, сколько падает
   - Формат: "📊 Распределение: 8 на дне (потенциал покупки), 5 на пике (готовы к продаже)"

6. **Добавить колонки в Invest для отслеживания:**
   - "Последнее уведомление" (дата/время)
   - "Статус уведомления" (отправлено/не отправлено)
   - "Изменение за день" (в %)
   - Это поможет собирать более детальную статистику для отчетов

---

### Схема работы триггеров:

```
1. Триггер времени (00:10 или 12:00)
   ↓
2. history_createPeriodAndUpdate_morning/evening()
   ↓
3. unified_priceUpdate() (каждые 10 минут)
   ↓
4. history_updatePricesForPeriod()
   ↓
5. syncPricesFromHistoryToInvestAndSales()
   ↓
6. telegram_checkPriceTargets() ← УВЕДОМЛЕНИЯ О ЦЕНАХ
   ↓
7. Если период завершен:
   ↓
8. invest_dailyReset()
   ↓
9. portfolioStats_update()
   ↓
10. telegram_sendDailyReport() ← ЕЖЕДНЕВНЫЙ ОТЧЕТ
```

---

### Важные моменты:

- ⚠️ **Telegram должен быть настроен**: Если Bot Token и Chat ID не настроены, функции просто выходят без ошибок
- ⚠️ **Частота уведомлений**: Уведомления о ценах могут приходить каждые 10 минут (если условия выполняются)
- ✅ **Ежедневный отчет**: Отправляется только один раз в день (после завершения сбора цен)
- ✅ **Безопасность**: Токен и Chat ID хранятся в `PropertiesService` (не видны в коде)
## Текущие задачи
---

## Идеи для будущего (не приоритетные)

### История успешных сделок и анализ
- **Что**: Анализ проданных предметов из Sales для улучшения стратегии
- **Метрики**:
  - Средняя прибыльность проданных позиций
  - Среднее время удержания позиции
  - Какие фазы/тренды дали лучший результат
  - Процент успешных сделок
- **Реализация**: Новый лист "Analytics" с формулами, анализирующими Sales

**Комментарий пользователя**: "Интересно пока думаем"

---

### Визуализация трендов
- **Что**: Автоматическое создание графиков для выбранных предметов
- **Варианты**:
  - График цены за период (используя встроенные графики Google Sheets)
  - График прибыльности портфеля
  - Сравнение нескольких предметов
- **Реализация**: Функция, создающая графики через `insertChart()` или рекомендации по ручному созданию

**Комментарий пользователя**: "Интересно, пока думаем"

---

### Дополнительные метрики в History
- **Что**: Новые колонки с полезной аналитикой
- **Метрики**:
  - Волатильность (стандартное отклонение цен за период)
  - Средняя цена за 7/30 дней
  - Изменение цены за период (%)
  - Максимальная просадка от пика
- **Реализация**: Новые функции расчета в `History.gs`, новые колонки

**Комментарий пользователя**: "Пока думаем, нужны только реально применимые данные в такой экономике как стим"

---

### Умные рекомендации на основе истории
- **Что**: Анализ прошлых успешных сделок для улучшения рекомендаций
- **Логика**:
  - Если предмет с похожими характеристиками (фаза, тренд, потенциал) дал прибыль в прошлом → повысить приоритет
  - Учет времени удержания позиции
  - Учет волатильности предмета
- **Реализация**: Машинное обучение (простое) или правила на основе истории Sales

**Комментарий пользователя**: "Интересно, пока думаем"

---

### Оптимизация производительности

**Текущее состояние:**
- ✅ **Уже реализовано:**
  - Batch-операции для чтения/записи данных
  - Оптимизированы `history_updateTrends()`, `history_updateCurrentPriceMinMax_()`
  - Оптимизированы функции синхронизации - batch-чтение из History
  - Оптимизировано форматирование колонок дат
  - Оптимизировано обновление изображений в History (убрана задержка 1000мс)

- ⚠️ **Потенциальные улучшения:**
  - **Кеширование**: Использовать `CacheService` для кеширования данных History (5-10 минут)
  - **Ленивая загрузка**: Загружать данные только для нужных предметов
  - **Условное форматирование**: Проверить, не создаются ли лишние правила

- 📊 **Оценка**: Текущая оптимизация уже значительно улучшила скорость. Основные узкие места - API-запросы к Steam Market (не оптимизируются).

---
