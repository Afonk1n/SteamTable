# ЗАДАЧИ И ИНСТРУКЦИИ

> **Правила работы с документацией и кодом** - см. `.cursor/rules/code-doc-rules.mdc` и `Documentation.md`

---

## ✅ ВЫПОЛНЕНО: Переработка системы аналитики героев (Immortal + Мета сигнал)

**Статус:** Завершено

**Выполнено:**
- ✅ OpenDotaAPI.gs: High Rank изменен с 6+7+8 на только Immortal (ранг 8)
- ✅ HeroStats.gs: Добавлена функция heroStats_getStats24HoursAgo() и обновлены расчеты (убрано contestRateChange7d, добавлено pickRateChange24h)
- ✅ Constants.gs: Обновлены веса HERO_TREND_SCORE, добавлены META_SIGNAL веса, обновлены столбцы и заголовки
- ✅ Analytics.gs: Переработан analytics_calculateHeroTrendScore(), созданы analytics_calculateMetaSignal() и analytics_formatMetaSignal()
- ✅ History.gs: Убраны старые столбцы, добавлены новые (Pro Contest Rate, Pick Rate Change 24h, Мета сигнал)
- ✅ Invest.gs: Добавлен расчет и отображение Мета сигнала
- ✅ Sales.gs: Добавлены новые столбцы и расчет Мета сигнала
- ✅ Telegram.gs: Добавлена функция telegram_checkMetaSignalOpportunities_() для горячих уведомлений
- ✅ Documentation.md: Обновлена документация (Hero Trend Score, Мета сигнал, структура столбцов)

**Детали:** См. раздел "💭 МОИ МЫСЛИ И ПРЕДЛОЖЕНИЯ" ниже

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### 4. Улучшение fallback механизма для проблемных предметов

**Проблема:**
- 9-11 предметов не получают Min/Max из-за ошибок Steam API
- Текущий fallback (Steam Market API + исторические данные) не всегда работает
- Нужен более умный механизм с задержками и повторными попытками

**Анализ:**
- Steam API может часто возвращать ошибки, к нему нужно по-особому стучаться
- Некоторые предметы могут требовать специальной обработки (например, с задержками)
- Прямая ссылка на Steam Market может быть более надежной для некоторых предметов

**Решение:**
- [ ] Разработать отдельный механизм для проблемных предметов
- [ ] Добавить умные задержки между запросами для Steam API (экспоненциальная задержка)
- [ ] Реализовать механизм повторных попыток с разными стратегиями:
  - Повтор через SteamWebAPI.ru с задержкой
  - Повтор через item_by_nameid с задержкой
  - Повтор через Steam Market API с задержкой
- [ ] Рассмотреть возможность прямого обращения к Steam Market через ссылку предмета
- [ ] Добавить кэширование неудачных попыток, чтобы не повторять их часто
- [ ] Логировать проблемные предметы для последующего анализа

**Комментарий:** Это улучшит покрытие Min/Max для всех предметов, включая проблемные.

---

## ✅ ИСПРАВЛЕНО: Проблемы с данными героев

**Проблемы:**
- Contest Rate и Pick Rate были одинаковыми или Contest Rate > 100%
- Contest Rate Change (7д) всегда 0%
- Непонятно, к какому рангу относятся данные

**Исправления:**
- [x] Исправлен расчет Contest Rate (теперь равен Pick Rate, так как OpenDota API не предоставляет banRate для обычных матчей)
- [x] Исправлено форматирование процентов (теперь правильно отображаются)
- [x] Обновлены заголовки колонок (убрано "(текущий)", добавлено "(High Rank)")

**Как интерпретируются данные:**

1. **Pick Rate (1.4%)** - процент пиков героя от всех пиков в High Rank (Ancient+Divine+Immortal)
   - Пример: 1.4% = герой был выбран в 1.4% всех матчей High Rank

2. **Contest Rate (1.4%)** - равен Pick Rate, так как OpenDota API не предоставляет banRate для обычных матчей
   - Баны доступны только для про-матчей (proBan)

3. **Contest Rate Change (7д)** - изменение Contest Rate за 7 дней в процентах
   - 0% = нет данных за 7 дней назад (нужно минимум 2 обновления)
   - Положительное значение = герой становится популярнее
   - Отрицательное значение = герой теряет популярность

4. **Win Rate (52.02%)** - процент побед героя в матчах
   - >50% = сильный герой
   - <50% = слабый герой

5. **Все данные относятся к High Rank** (Ancient + Divine + Immortal ранги)

Ну ладно, нужно значить немного переработать аналитику, зачем нам два столбца которые одно и тоже показывают и в самой аналитике нужно учесть то что у нас нет контест рейта

Во первых меня теперь озадачил вопрос того правильно ли мы вообще используем данные, нет ли негде неправильных значений в расчётах или неправильной подставноке, понимаешь это же самый главный смысл таблицы произвести верные вычисления, основываясь на них я собираюсь инвестировать свои деньги, поэтому тебе нужно применить максимальную экономическую, математическую, аналитическую и подобные компетенции в этом вопросе.

Теперь к контекст рейту, нам как минимум нужно будет убрать или заменить столбец в таблице, зачем нам выводить два пикрейта но сначала такой вопрос
можем ли мы сделать что хай птс будет считать не Ancient+Divine+Immortal, а Divine+Immortal это больше похоже на правду по дотерским меркам
Ещё у нас есть контест рейт про игроков, который по идеи тоже будет работать просто там маленькая выборка, поресёрчи инфу про доту и чекни там апишку опендоты и предложи варианты что нам сделать, как переделать нашу аналитику с учётом всего этого

---

## 📊 АНАЛИЗ: Данные OpenDota API и предложения по аналитике

### Доступные данные из OpenDota API:

**1. Статистика по рангам (для каждого героя):**
- `1_pick`, `1_win` - Herald (ранг 1)
- `2_pick`, `2_win` - Guardian (ранг 2)
- `3_pick`, `3_win` - Crusader (ранг 3)
- `4_pick`, `4_win` - Archon (ранг 4)
- `5_pick`, `5_win` - Legend (ранг 5)
- `6_pick`, `6_win` - Ancient (ранг 6)
- `7_pick`, `7_win` - Divine (ранг 7)
- `8_pick`, `8_win` - Immortal (ранг 8)
Наверное стоит даже за хай птс считать только иммротал, там все равно щас до 8500 наверное только матчи, выборка будет по меньше но эни вей думаю лучше так разделить

**2. Про-статистика (для каждого героя):**
- `pro_pick` - количество пиков в про-матчах
- `pro_ban` - количество банов в про-матчах
- `pro_win` - количество побед в про-матчах
- **Важно:** `proContestRate = proPick + proBan` (полный контест-рейт для про-матчей)
Это отлично, очень важная характеристика
**3. Дополнительные данные:**
- `turbo_picks`, `turbo_wins` - статистика для Turbo режима
- `win_rate` - можно рассчитать из `wins / picks`
Турбо нам не нужно
**4. Ограничения API:**
- ❌ **НЕТ banRate для обычных матчей** (только для про-матчей)
- ❌ **НЕТ contestRate для обычных матчей** (только pickRate)
- ✅ **ЕСТЬ полный contestRate для про-матчей** (proPick + proBan)

---

### 🔍 Проблемы в текущей аналитике:

**1. Contest Rate = Pick Rate (дублирование) - ПОДРОБНОЕ ОБЪЯСНЕНИЕ:**

**Проблема:**
- В коде `contestRate = pickRate` (так как OpenDota API не дает banRate для обычных матчей)
- Поэтому `contestRateChange7d` = `pickRateChange7d` (одинаковые значения!)
- Но в аналитике используются ОБА с разными весами:
  - `CONTEST_RATE_CHANGE: 0.40` (использует `contestRateChange7d`)
  - `PICK_RATE: 0.25` (использует `pickRate`)
- **Результат:** один и тот же показатель учитывается дважды с разными весами!

**Пример:**
```
Герой X:
- pickRateChange7d = +10% (рост популярности)
- contestRateChange7d = +10% (то же самое!)

В формуле:
score = (10% * 0.40) + (10% * 0.25) + ... = 4% + 2.5% + ...
```

**Решение:**
- Убрать `contestRateChange7d` полностью (фейк)
- Оставить только `pickRateChange7d` (реальные данные)
- Освободившийся вес `0.40` перераспределить на `PRO_CONTEST_RATE_CHANGE` (водопадная мета)
**2. banRate всегда = 0**
- В `Hero Trend Score` используется `banRate` с весом `0.10`
- Но `banRate` всегда 0 для обычных матчей
- Этот компонент не работает
Ну получается уберём это

**3. High Rank = Ancient+Divine+Immortal**
- Пользователь хочет Divine+Immortal (более точная метрика)
- Ancient может быть слишком "низким" для инвестиционных решений
Выше отписал берём только иммортал
**4. Pro Contest Rate используется, но выборка мала**
- `proContestRateChange7d` имеет вес `0.20` в аналитике
- Но про-матчей меньше, чем обычных, поэтому данные менее стабильны
Тут скорее всего вес нужно будет повысить, по сути в доте водопадная мета которая начинается на про сцене (+ сейчас турнры идут друг за другом, поэтому матчей будет думаю достаточно), потом тренд подхватывают на хай птс и заканчивают на лоу птс
---

### 💡 Предложения по переработке (с учетом комментариев):

**Ключевые изменения:**
1. **High Rank = только Immortal** (ранг 8) - более точная метрика для инвестиций
2. **Pro Contest Rate** - приоритетный индикатор (водопадная мета: про → Immortal → остальные)
3. **Убрать Contest Rate** (фейк, дублирует Pick Rate)
4. **Убрать banRate** (всегда 0 для обычных матчей)

---

### 📊 Текущие веса Hero Trend Score:

```javascript
CONTEST_RATE_CHANGE: 0.40  // ❌ ФЕЙК (равен pickRateChange)
PRO_CONTEST_RATE_CHANGE: 0.20
PICK_RATE: 0.25
BAN_RATE: 0.10  // ❌ НЕ РАБОТАЕТ (всегда 0)
WIN_RATE: 0.05
Итого: 1.00
```

**Освобождается:** `0.40 + 0.10 = 0.50` (после удаления фейков)

---

### 🎯 РЕКОМЕНДУЕМАЯ ФОРМУЛА (с учетом водопадной меты):

**Новые веса Hero Trend Score:**

```javascript
PRO_CONTEST_RATE_CHANGE: 0.45  // ↑ Было 0.20, стало 0.45 (водопадная мета!)
PICK_RATE_CHANGE_IMMORTAL: 0.30  // ↑ Новый (вместо contestRateChange)
PICK_RATE_IMMORTAL: 0.20  // ↓ Было 0.25, стало 0.20
WIN_RATE: 0.05  // Без изменений
Итого: 1.00
```
Я бы хотел ещё как минимум в таблицах бы хотел чётко видеть контест рейт просцены в столбцах и возможно прирост контест рейта на просцене за неделю и прирост пикрейта на хайптс (иммортал) за какой то короткий период типо сутки или несколько и тебе стоит подумать возможно эти данные должны тоже учитываться при оценке хиро тренд скор
Прирост пикрейта на хайп птс за короткий промежуток поможет в случае если выйдет патч и какой то герой будет новой имбой, люди на хай птс начнут быстро пикать его и это может быть хорошим сигналом для покупку, но в общей картине не знаю какой вес это могло бы заслужить, тут наверное больше полезен столбец у меня на глазах
В общем подумай над всем что я туту написал, распишешь тоже свои мысли

Пришла новая мысль я щас всё таки прочитаю твои предложения прокомментарую но пока идёт мысль
Чтобы нам не засирать нашу фундоментальную инвестиционную оценку можно было бы реализовать отдельнй параметр и столбец типо Мета сигнал который был бы ориентирован больше на изменения параметров связанных с героем в краткосрочной перспектике (не знаю сильно ли это отличается от хиротренда, тут мне следует изучить вопрос) и во время патча допустим я бы просто ориентировался по этому столбцу и дополнял своё понимание уже другими показателями

Плюс этот столбец мог бы послужить исходными данными для новых вариантов телеграмм сообщений, такие горячие рекомендации, но там комбо интересовало бы типо шмотки сразу на растущего героя

✅ **УЧТЕНО** - Добавлен раздел "Мета сигнал" ниже

---

## 💭 МОИ МЫСЛИ И ПРЕДЛОЖЕНИЯ:

### 🎯 НОВАЯ КОНЦЕПЦИЯ: "Мета сигнал" (отдельный параметр)

**Идея пользователя:**
- Отдельный столбец "Мета сигнал" для краткосрочных изменений героя
- Не засирать фундаментальную инвестиционную оценку (Hero Trend остается как есть)
- Во время патча ориентироваться по этому столбцу
- Использовать для телеграм-уведомлений (горячие рекомендации, комбо: шмотки на растущего героя)

**Отличие от Hero Trend:**
- **Hero Trend** - фундаментальная оценка (недельные тренды, про-мета, текущая популярность) - "на дистанцию"
- **Мета сигнал** - краткосрочный индикатор (24h изменения, патч-имбы, быстрые сигналы) - "быстрый"

✅ **УЧТЕНО** - Два разных индикатора: один на дистанцию (Hero Trend), один быстрый (Мета сигнал). Ниже предложение по взаимодействию.
Ну вот как раз хотел высказать мысль что по сути нужно иметь 2 разных индикатор для героев, один на дистанцию и один быстрый, можно продумать даже какое нибудь взаимодействие мета сигнала с хиротрендом не знаю если это не избыточно


**Формула "Мета сигнала":**

```javascript
META_SIGNAL_WEIGHTS: {
  PICK_RATE_CHANGE_IMMORTAL_24H: 0.50  // Главный индикатор патч-имб
  PRO_CONTEST_RATE_CHANGE_7D: 0.30    // Про-мета (водопадная)
  PICK_RATE_CHANGE_IMMORTAL_7D: 0.20  // Недельный тренд Immortal
  Итого: 1.00
}
✅ **УЧТЕНО** - Формула одобрена
да выглядит неплохо
```

**Обоснование:**
- Фокус на краткосрочных изменениях (24h = 50%)
- Быстрая реакция на патчи (когда они выходят - критически важно)
- Не влияет на фундаментальную оценку (Hero Trend остается отдельно)
- Легко визуализировать и использовать для быстрых решений

**Реализация:**
- Новый столбец "Мета сигнал" в History (и возможно Invest/Sales)
- Отдельная функция `analytics_calculateMetaSignal()`
- Использовать для телеграм-уведомлений (горячие рекомендации)
- Условное форматирование: резкие изменения = яркий цвет

✅ **УЧТЕНО** - Формат: эмодзи + число (0-100). Обычно низкий, во время патча повышается. Ниже детали реализации.
Можно также эмодзи и число в 100 бальной системе тогда наверное получится так что этот параметр будет обычно низкий но во время патча будет повышаться

**Формат отображения "Мета сигнала":**
```javascript
// Форматирование аналогично Investment Score
function analytics_formatMetaSignal(score) {
  // score: 0-100
  if (score >= 75) return `🔥 ${Math.round(score)}`  // Горячий сигнал (патч!)
  if (score >= 60) return `🟡 ${Math.round(score)}`  // Средний
  if (score >= 40) return `⚪ ${Math.round(score)}`  // Низкий
  return `🔴 ${Math.round(score)}`  // Очень низкий
}
```

**Взаимодействие Мета сигнала с Hero Trend:**

**Вариант 1: Визуальное объединение (РЕКОМЕНДУЕТСЯ)**
- В столбце Hero Trend показывать: `🟢 85 | 🔥 92`
- Hero Trend (фундаментальный) | Мета сигнал (быстрый)
- Легко видеть оба индикатора одновременно

**Вариант 2: Отдельные столбцы**
- Hero Trend в своем столбце
- Мета сигнал в отдельном столбце
- Больше места, но проще читать
Точно отдельно, мета сигнал можно последним среди всех характеристик
**Вариант 3: Бонус к Investment Score (опционально)**
- Если Мета сигнал > 70 И Hero Trend > 60 → +10 к Investment Score
- Учитывает краткосрочные сигналы без изменения весов
- Используется только для телеграм-уведомлений (горячие рекомендации)
Не понял что значит только для телеграмм уведомлений используется если предлагаешь сделать бонус к инвестмент скор, но бонус я думаю не стоит, но вот сценарий отправки уведомления в тг при наборе проедметом определённого значения точно нужно будет
---

### 📊 Пояснение существующих параметров:

**1. Hero Trend (Тренд героя):**
- **Что это:** Фундаментальная оценка тренда героя (0-100)
- **Как работает:** 
  - Учитывает недельные изменения (7d)
  - Про-мету (proContestRateChange)
  - Текущую популярность (pickRate)
  - Win Rate
- **Использование:** Входит в Investment Score с весом 35% (для Hero Items)
- **Где видно:** Столбец "Hero Trend" в History/Invest/Sales

✅ **УЧТЕНО** - Веса Hero Trend Score добавлены ниже
Мне нужно увидеть веса

**Текущие веса Hero Trend Score (из Constants.gs):**
```javascript
HERO_TREND_SCORE: {
  CONTEST_RATE_CHANGE: 0.40,  // ❌ Будет удалено (фейк)
  PRO_CONTEST_RATE_CHANGE: 0.20,
  PICK_RATE: 0.25,
  BAN_RATE: 0.10,  // ❌ Будет удалено (не работает)
  WIN_RATE: 0.05
}
```

**Новые веса Hero Trend Score (после переработки):**
```javascript
HERO_TREND_SCORE: {
  PRO_CONTEST_RATE_CHANGE_7D: 0.40,  // Про-мета за неделю
  PICK_RATE_CHANGE_IMMORTAL_7D: 0.25,  // Immortal за неделю
  PICK_RATE_IMMORTAL: 0.20,  // Текущая популярность
  WIN_RATE: 0.05  // Вспомогательный
  // PICK_RATE_CHANGE_IMMORTAL_24H убран (переходит в Мета сигнал)
}
```
вроде круто

**2. Risk Level (Уровень риска):**
- **Что это:** Уровень риска инвестиции (Низкий/Средний/Высокий)
- **Как работает:**
  - Если Investment Score >= 70 И Volatility < 0.5 И Demand Ratio > 0.6 → "Низкий"
  - Если Investment Score >= 50 И Volatility < 0.7 И Demand Ratio > 0.4 → "Средний"
  - Иначе → "Высокий"
- **Использование:** Показывает рискованность инвестиции
- **Где видно:** Столбец "Уровень риска" в History/Invest/Sales

**3. Investment Score (Оценка инвестиции):**
- **Что это:** Итоговая оценка для покупки (0-100)
- **Как работает:**
  - Hero Trend (35%) + Volatility (20%) + Demand Ratio (20%) + Price Momentum (15%) + Liquidity (5%) + Sales Trend (5%)
  - Для Hero Items учитывает Hero Trend
  - Для Common Items - без Hero Trend
- **Использование:** Основной индикатор для покупки
- **Где видно:** Столбец "Investment Score" в History/Invest

✅ **УЧТЕНО** - Анализ баланса весов Investment Score ниже
Тут сам уже подумай относительно всех моих мыслей стоит ли поменять баланс весов или нет

**Текущие веса Investment Score:**
```javascript
INVESTMENT_SCORE: {
  HERO_TREND: 0.35,  // Фундаментальная оценка героя
  VOLATILITY: 0.20,  // Волатильность цены
  DEMAND_RATIO: 0.20,  // Соотношение спроса/предложения
  PRICE_MOMENTUM: 0.15,  // Импульс цены
  LIQUIDITY: 0.05,  // Ликвидность
  SALES_TREND: 0.05  // Тренд продаж
}
```

**Анализ и предложения:**

**Вариант 1: Оставить как есть (РЕКОМЕНДУЕТСЯ)**
- Hero Trend (35%) - правильный вес для фундаментальной оценки
- Остальные веса сбалансированы
- Мета сигнал будет отдельно (не влияет на Investment Score)

**Вариант 2: Добавить Мета сигнал в Investment Score (НЕ РЕКОМЕНДУЕТСЯ)**
- Может перегрузить формулу
- Мета сигнал лучше использовать отдельно для быстрых решений

**Вариант 3: Взаимодействие Мета сигнала с Investment Score**
- Если Мета сигнал > 70 И Hero Trend > 60 → бонус +10 к Investment Score
- Это не меняет веса, но учитывает краткосрочные сигналы
- Можно использовать для телеграм-уведомлений (горячие рекомендации)
ЩАс понял уже про тг, ну я думаю проще мета сигнал использовать как отдельный кейс когда он будет являться поводом для определённого уведа
---

## 💭 МОИ МЫСЛИ И ПРЕДЛОЖЕНИЯ:

### 📊 Новые столбцы в таблице History:

**1. Pro Contest Rate (текущий)** - столбец
- Показывает актуальный контест-рейт про-сцены (proPick + proBan)
- Важный индикатор текущей про-меты
- **Формат:** процент (например, 45.2%)

**2. Pro Contest Rate Change (7d)** - столбец
- Прирост контест-рейта на про-сцене за неделю
- Показывает тренд в про-мете
- **Формат:** процент изменения (например, +15.3%)

**3. Pick Rate Change Immortal (24h или 3d)** - столбец
- Прирост пикрейта на Immortal за короткий период
- Критически важен при выходе патчей (новая имба)
- **Рекомендация:** использовать **24 часа** (быстрее реагирует на патчи)
- **Формат:** процент изменения (например, +25.5%)

✅ **УЧТЕНО** - Анализ размещения столбцов в Invest и Sales ниже
Наверное хотелось бы видеть такие столбцы и в invest и sales только не знаю как всё уместить, ну в sales есть место, а вот в invest не знаю как быть

**Анализ размещения столбцов:**

**Invest (сейчас 21 колонка A-U):**
- A-K: Основные данные (11 колонок)
- L-M: Min/Max (2 колонки)
- N: Investment Score
- O: Рекомендация
- P: Фаза
- Q: Потенциал
- R: Тренд
- S: Hero Trend
- T: Risk Level
- U: Чекбокс "Продать"

**Предложение для Invest:**
- Можно добавить после Risk Level (T):
  - V: **Мета сигнал** ⭐ (если критически важно)
  - Или объединить с Hero Trend (S) - показать оба значения в одной ячейке
  - Или добавить как скрытую колонку (для расчетов, но не отображать)
  я точно хочу видеть мета сигнал и отдельно, остальные столбц видимо тут не будет добавлять

**Sales (сейчас 13 колонок A-M):**
- A-B: Изображение/Название (2 колонки)
- C-F: Цены и просадка (4 колонки)
- G: Ссылка
- H-I: Min/Max (2 колонки)
- J: Buyback Score
- K: Рекомендация
- L: Hero Trend
- M: Risk Level

**Предложение для Sales:**
- Есть место после Risk Level (M):
  - N: **Pro Contest Rate (current)**
  - O: **Pro Contest Rate Change (7d)**
  - P: **Pick Rate Change Immortal (24h)**
  - Q: **Мета сигнал**
да ок
**Рекомендация:**
- **Sales:** Добавить все новые столбцы (есть место)
- **Invest:** Добавить только "Мета сигнал" (V) или объединить с Hero Trend
- **History:** Добавить все столбцы (O-U)
Да, так и поступим
---

### 🎯 Обновленная формула Hero Trend Score:

**Вариант 1: С коротким периодом (РЕКОМЕНДУЕТСЯ)**

```javascript
PRO_CONTEST_RATE_CHANGE_7D: 0.40  // Про-мета за неделю (водопадная мета)
PICK_RATE_CHANGE_IMMORTAL_7D: 0.25  // Immortal за неделю
PICK_RATE_CHANGE_IMMORTAL_24H: 0.15  // ⭐ НОВЫЙ: Immortal за 24ч (патчи!)
PICK_RATE_IMMORTAL: 0.15  // Текущая популярность
WIN_RATE: 0.05  // Вспомогательный
Итого: 1.00
```

**Обоснование:**
- `PICK_RATE_CHANGE_IMMORTAL_24H: 0.15` - небольшой, но важный вес
- Помогает быстро уловить патч-имбы (резкий скачок за 24ч)
- Не перегружает формулу (15% - достаточно для сигнала)
- Основной вес остается на про-мете (40%) и недельных трендах (25%)

**Вариант 2: Без короткого периода (если только для визуализации)**

```javascript
PRO_CONTEST_RATE_CHANGE_7D: 0.45  // Про-мета за неделю
PICK_RATE_CHANGE_IMMORTAL_7D: 0.30  // Immortal за неделю
PICK_RATE_IMMORTAL: 0.20  // Текущая популярность
WIN_RATE: 0.05  // Вспомогательный
Итого: 1.00
```

**Моя рекомендация:** **Вариант 1** - включить 24h в формулу с весом 0.15, так как:
- Патчи выходят регулярно
✅ **УЧТЕНО** - Патчи выходят редко (203 дня с последнего), но когда выходят - критически важны. Поэтому 24h сигнал нужен, но как отдельный "Мета сигнал" (см. ниже)
- Новая имба = быстрый рост цены
- 24h сигнал может дать преимущество в покупке
- Вес 0.15 не перегружает формулу, но дает важный сигнал

---

### 📋 Структура новых столбцов в History:

**Текущая структура (после удаления Contest Rate):**
- O: Contest Rate Change (7d) → **УДАЛИТЬ** (фейк)
- P: Contest Rate (current) → **УДАЛИТЬ** (фейк)
- Q: Pick Rate (Immortal)
- R: Win Rate

**Новая структура (обновлено с учетом комментария):**
- O: **Pro Contest Rate (current)** ⭐ НОВЫЙ
- P: **Pro Contest Rate Change (7d)** ⭐ НОВЫЙ
- Q: Pick Rate Change Immortal (7d) - переименовать из "Contest Rate Change"
- R: **Pick Rate Change Immortal (24h)** ⭐ НОВЫЙ
- S: Pick Rate (Immortal) - переименовать из "Pick Rate (current)"
- T: Win Rate
- U: **Мета сигнал** ⭐ НОВЫЙ (отдельный параметр для краткосрочных изменений)

✅ **УЧТЕНО** - Порядок столбцов обновлен (Q, R, S). Добавлено объяснение Hero Trend и Risk Level выше. Добавлен раздел "Мета сигнал"
---

### 🔧 Технические детали реализации:

**1. HeroStats.gs - добавить расчет pickRateChange24h:**

```javascript
// Получаем статистику 24 часа назад
const stats24HoursAgo = heroStats_getStats24HoursAgo(heroStat.heroId, 'High Rank')

// Рассчитываем pickRateChange24h
let pickRateChange24h = 0
if (stats24HoursAgo && stats24HoursAgo.pickRatePercent !== undefined && stats24HoursAgo.pickRatePercent > 0) {
  const currentPickRatePercent = heroStat.pickRatePercent || 0
  pickRateChange24h = ((currentPickRatePercent - stats24HoursAgo.pickRatePercent) / stats24HoursAgo.pickRatePercent) * 100
  
  // ВАЛИДАЦИЯ: Ограничиваем изменения
  const MAX_CHANGE_PERCENT = 1000
  const MIN_CHANGE_PERCENT = -1000
  if (pickRateChange24h > MAX_CHANGE_PERCENT || pickRateChange24h < MIN_CHANGE_PERCENT) {
    pickRateChange24h = pickRateChange24h > 0 ? MAX_CHANGE_PERCENT : MIN_CHANGE_PERCENT
  }
}

// Добавить в statsData:
pickRateChange24h: pickRateChange24h
```

**2. HeroStats.gs - добавить функцию `heroStats_getStats24HoursAgo()`:**

```javascript
function heroStats_getStats24HoursAgo(heroId, rankCategory) {
  // Аналогично heroStats_getStats7DaysAgo, но для 24 часов
  // Ищем запись с timestamp примерно 24 часа назад
}
```

**3. History.gs - добавить новые столбцы:**

```javascript
// Новые колонки
const proContestRateCol = getColumnIndex(HISTORY_COLUMNS.PRO_CONTEST_RATE_CURRENT)
const proContestRateChangeCol = getColumnIndex(HISTORY_COLUMNS.PRO_CONTEST_RATE_CHANGE_7D)
const pickRateChange24hCol = getColumnIndex(HISTORY_COLUMNS.PICK_RATE_CHANGE_IMMORTAL_24H)

// В цикле подготовки данных:
proContestRates.push([stats.proContestRate ? stats.proContestRate / 100 : 0])
proContestRateChanges.push([stats.proContestRateChange7d ? stats.proContestRateChange7d / 100 : 0])
pickRateChanges24h.push([stats.pickRateChange24h ? stats.pickRateChange24h / 100 : 0])
```

**4. Analytics.gs - обновить Hero Trend Score:**

```javascript
const pickRateChange24h = stats.pickRateChange24h || 0

// Нормализация (24h изменения могут быть более резкими)
const pickRateChange24hNorm = analytics_normalizeToRange(pickRateChange24h, -0.5, 0.5, 0, 1)

// В формуле:
const score = 
  (proContestRateChangeNorm * 0.40) +
  (pickRateChange7dNorm * 0.25) +
  (pickRateChange24hNorm * 0.15) +  // ⭐ НОВЫЙ
  (pickRateNorm * 0.15) +
  (winRateNorm * 0.05)
```

**5. Constants.gs - обновить:**

```javascript
HISTORY_COLUMNS: {
  // ...
  PRO_CONTEST_RATE_CURRENT: 'O',
  PRO_CONTEST_RATE_CHANGE_7D: 'P',
  PICK_RATE_CHANGE_IMMORTAL_7D: 'Q',
  PICK_RATE_CHANGE_IMMORTAL_24H: 'R',  // ⭐ НОВЫЙ
  PICK_RATE_IMMORTAL: 'S',
  WIN_RATE_CURRENT: 'T',
  // ...
}

HERO_TREND_SCORE: {
  PRO_CONTEST_RATE_CHANGE_7D: 0.40,
  PICK_RATE_CHANGE_IMMORTAL_7D: 0.25,
  PICK_RATE_CHANGE_IMMORTAL_24H: 0.15,  // ⭐ НОВЫЙ
  PICK_RATE_IMMORTAL: 0.15,
  WIN_RATE: 0.05
}
```

---

### ⚠️ Важные моменты:

1. **24h данные могут быть менее стабильными:**
   - Меньше выборка за 24 часа
   - Могут быть шумы
   - Но критически важны для патч-имб (когда патчи выходят - это редко, но важно)

2. **Fallback логика:**
   - Если `pickRateChange24h` отсутствует → вес перераспределяется на `PICK_RATE_CHANGE_IMMORTAL_7D`
   - Если `proContestRateChange7d` отсутствует → вес перераспределяется на `PICK_RATE_CHANGE_IMMORTAL_7D`

3. **Визуализация:**
   - Столбец `Pick Rate Change Immortal (24h)` должен быть выделен (условное форматирование)
   - Резкие изменения (+20%+) = красный/зеленый цвет
   - Помогает быстро увидеть патч-имбы

4. **"Мета сигнал" как отдельный параметр:**
   - Не влияет на Hero Trend (фундаментальная оценка остается)
   - Фокус на краткосрочных изменениях (24h = 50% веса)
   - Используется для быстрых решений во время патчей
   - Может служить для телеграм-уведомлений (горячие рекомендации)

**Обоснование весов (ОБНОВЛЕНО с учетом новых требований):**

1. **PRO_CONTEST_RATE_CHANGE_7D: 0.40** (главный индикатор)
   - Про-сцена задает тренды → водопадная мета
   - Сейчас турниры идут друг за другом → выборка достаточна
   - Полный контест-рейт (pick + ban) = более точная метрика
   - **Было:** 0.20 → **Стало:** 0.40 (+0.20)

2. **PICK_RATE_CHANGE_IMMORTAL_7D: 0.25** (второй по важности)
   - Immortal первыми подхватывают про-мету
   - Показывает ранние сигналы роста популярности за неделю
   - **Новый компонент** (заменяет фейк contestRateChange)

3. **PICK_RATE_CHANGE_IMMORTAL_24H: 0.15** ⭐ НОВЫЙ (третий по важности)
   - Критически важен для патч-имб
   - Быстрый сигнал при выходе патча (люди на Immortal быстро пикают)
   - Может дать преимущество в покупке до массового роста цены
   - **Новый компонент** (для визуализации + аналитики)

4. **PICK_RATE_IMMORTAL: 0.15** (текущая популярность)
   - Текущий пикрейт в Immortal ранге
   - Показывает актуальное состояние меты
   - **Было:** 0.25 → **Стало:** 0.15 (-0.10)

5. **WIN_RATE: 0.05** (вспомогательный)
   - Показывает силу героя, но не тренд
   - Оставляем минимальный вес

6. **Убрано:**
   - `CONTEST_RATE_CHANGE: 0.40` (фейк)
   - `BAN_RATE: 0.10` (не работает)

---

### 📋 Детальный план изменений:

**1. OpenDotaAPI.gs:**
- Изменить High Rank с `6+7+8` (Ancient+Divine+Immortal) на `8` (только Immortal)
- Обновить расчет `highRankPick` и `highRankWinRate`

**2. History.gs:**
- Убрать столбец "Contest Rate (High Rank)"
- Оставить только "Pick Rate (Immortal)"
- Обновить заголовки

**3. Analytics.gs - `analytics_calculateHeroTrendScore()`:**
```javascript
// БЫЛО:
const contestRateChange7d = stats.contestRateChange7d || 0  // ❌ Фейк
const banRate = stats.banRate || 0  // ❌ Всегда 0

// СТАНЕТ:
const pickRateChange7d = stats.pickRateChange7d || 0  // ✅ Immortal
const proContestRateChange7d = stats.proContestRateChange7d || 0  // ✅ Про
// banRate убрать полностью
```

**4. Constants.gs - новые веса:**
```javascript
HERO_TREND_SCORE: {
  PRO_CONTEST_RATE_CHANGE: 0.45,  // ↑ Было 0.20
  PICK_RATE_CHANGE: 0.30,  // ↑ Новый (вместо CONTEST_RATE_CHANGE: 0.40)
  PICK_RATE: 0.20,  // ↓ Было 0.25
  WIN_RATE: 0.05  // Без изменений
  // BAN_RATE убрать
}
```

**5. HeroStats.gs:**
- Убедиться, что `pickRateChange7d` рассчитывается для Immortal (ранг 8)
- Проверить расчет `proContestRateChange7d`

---

### ⚠️ Важно проверить при реализации:

1. **Перераспределение весов:**
   - Сумма всех весов = 1.00
   - Если `proContestRateChange7d` отсутствует, вес перераспределяется на `PICK_RATE_CHANGE`

2. **Нормализация данных:**
   - `pickRateChange7d` и `proContestRateChange7d` в процентах (например, 15 = 15%)
   - Нормализация: `analytics_normalizeToRange(value, -0.3, 0.3, 0, 1)`
   - `pickRate` в процентах (например, 1.4 = 1.4%)
   - Нормализация: `analytics_normalizeToRange((pickRate - 50) / 50, -1, 1, 0, 1)`

3. **Fallback логика:**
   - Если `proContestRateChange7d` = 0 или отсутствует:
     - `proContestRateWeight = 0`
     - `pickRateChangeWeight = 0.30 + 0.45 = 0.75` (перераспределение)

4. **Валидация:**
   - Все значения в диапазоне 0-1 после нормализации
   - Итоговый score в диапазоне 0-1
   - Проверка на деление на ноль

---

### ⚠️ Важно проверить:

1. **Правильность расчетов:**
   - Все проценты правильно делятся на 100 перед записью в Google Sheets
   - Изменения за 7 дней рассчитываются корректно
   - Нормализация работает правильно

2. **Использование данных:**
   - `pickRatePercent` используется везде одинаково
   - `proContestRate` рассчитывается как `proPick + proBan`
   - `winRate` берется из правильного источника

3. **Валидация:**
   - Проверить, что все значения в допустимых диапазонах
   - Убедиться, что нет деления на ноль
   - Проверить обработку отсутствующих данных