# СИСТЕМА АНАЛИЗА ИНВЕСТИЦИОННЫХ ВОЗМОЖНОСТЕЙ

> **Назначение**: Единый источник правды о системе анализа - от получения данных до генерации результатов.

---

## 📊 ИСТОЧНИКИ ДАННЫХ

### 1. SteamWebAPI.ru
**Используется для:**
- Получения данных о предметах (цены, объемы, ликвидность)
- Автоопределения героев для предметов (tag5)
- Получения Min/Max цен из истории

**Основные параметры:**
- `pricelatest`, `pricelatestsell` - текущая цена
- `pricelatestsell7d`, `pricelatestsell30d` - цены за периоды
- `priceavg7d`, `priceavg30d` - средние цены
- `pricemin`, `pricemax` - минимальная/максимальная цена из истории
- `sold24h`, `sold7d`, `sold30d` - объемы продаж
- `buyordervolume`, `offervolume` - объемы спроса/предложения
- `hourstosold` - среднее время до продажи

### 2. OpenDota API
**Используется для:**
- Получения статистики героев (пикрейт, винрейт, банрейт, контестрейт)
- Про-статистики (proPick, proBan, proContestRate)

**Основные параметры:**
- `pickRate`, `winRate`, `banRate` - базовая статистика
- `contestRate` - процент участий в контестах (pick + ban)
- `proPick`, `proBan`, `proContestRate` - про-статистика
- `contestRateChange7d` - изменение контестрейта за 7 дней

### 3. History Sheet
**Используется для:**
- Хранения истории цен по датам
- Расчет трендов, фаз цикла, потенциала роста
- Синхронизации аналитики в Invest/Sales

### 4. HeroMapping Sheet
**Используется для:**
- Связи предметов с героями
- Определения категории предмета (Hero Item / Common Item)

---

## 🔄 ОБРАБОТКА ДАННЫХ

### Валидация цен
Все цены перед записью проходят валидацию через `validatePrice_()`:
- Тип данных: `number`
- Значение: конечное число (`Number.isFinite()`)
- Диапазон: > 0 и < 50000 ₽

### Группировка цен по дате
Все функции, работающие с историей цен, группируют цены по дате (игнорируя период "ночь"/"день"):
- Парсинг даты из заголовка (формат: "dd.MM.yy ночь" или "dd.MM.yy день")
- Группировка по ключу `dd.MM.yy`, беря **последнюю цену** за день
- Сортировка дат в хронологическом порядке

---

## 📈 РАСЧЕТ МЕТРИК

### 1. Liquidity Score (Оценка ликвидности)
**Источник:** SteamWebAPI.ru

**Формула:**
```
Liquidity Score = нормализация(
  (sold24h / maxSold24h * 0.35) +
  (sold7d / maxSold7d * 0.25) +
  (1 / (1 + offervolume/1000) * 0.25) +
  (1 / (1 + hourstosold/24) * 0.15)
)
```

**Диапазон:** 0-1 (высокая ликвидность = лучше)

### 2. Demand Ratio (Соотношение спроса и предложения)
**Источник:** SteamWebAPI.ru

**Формула:**
```
Demand Ratio = buyordervolume / (buyordervolume + offervolume + 1)
```

**Диапазон:** 0-1 (высокий спрос = лучше)

### 3. Price Momentum (Импульс цены)
**Источник:** SteamWebAPI.ru + History

**Формула:**
```
Price Momentum = нормализация(
  ((currentPrice - price7d) / price7d * 0.5) +
  ((currentPrice - price30d) / price30d * 0.3) +
  ((currentPrice - priceavg7d) / priceavg7d * 0.2)
)
```

**Диапазон:** 0-1 (рост цены = лучше)

### 4. Sales Trend (Тренд продаж)
**Источник:** SteamWebAPI.ru

**Формула:**
```
Sales Trend = нормализация(
  ((sold24h * 7 - sold7d) / sold7d * 0.5) +
  ((sold7d * 4 - sold30d) / sold30d * 0.5)
)
```

**Диапазон:** 0-1 (рост продаж = лучше)

### 5. Volatility Index (Индекс волатильности)
**Источник:** History + SteamWebAPI.ru

**Формула:**
```
Volatility = (rangeVolatility * 0.5) + (stdDevVolatility * 0.5)
где:
  rangeVolatility = (maxPrice - minPrice) / avgPrice
  stdDevVolatility = stdDev / avgPrice
```

**Диапазон:** 0-1 (высокая волатильность = лучше, ПРЯМАЯ нормализация)

### 6. Hero Trend Score (Оценка тренда героя)
**Источник:** OpenDota API (HeroStats)

**Формула:**
```
Hero Trend Score = 
  (contestRateChangeNorm * 0.40) +
  (proContestRateChangeNorm * 0.20) +
  (pickRateNorm * 0.25) +
  (banRateNorm * 0.10) +
  (winRateNorm * 0.05)
```

**Примечание:** Если про-статистика недоступна, вес перераспределяется на contestRateChange (0.40 + 0.20 = 0.60)

**Диапазон:** 0-1 (рост меты = лучше)

---

## 🎯 РАСЧЕТ СКОРОВ

### Investment Score (Оценка для покупки)
**Диапазон:** 0-100 (целое число)

**Для Hero Items:**
```
Investment Score = 
  (Hero Trend Score * 0.35) +
  (Volatility Index * 0.20) +
  (Demand Ratio * 0.20) +
  (Price Momentum * 0.15) +
  (Liquidity Score * 0.05) +
  (Sales Trend * 0.05)
  
+ Корреляции:
  - Если Hero Trend > 0.6 и Price Momentum > 0.5: × 1.2
  - Если Demand Ratio > 0.7 и Liquidity Score > 0.6: × 1.15
```

**Для Common Items:**
```
Investment Score = 
  (Volatility Index * 0.30) +
  (Demand Ratio * 0.25) +
  (Price Momentum * 0.20) +
  (Sales Trend * 0.15) +
  (Liquidity Score * 0.10)
```

**Результат:** Округляется до целого числа (0-100)

### Buyback Score (Оценка для откупа)
**Диапазон:** 0-100 (целое число)

**Формула:**
```
priceDropPercent = (sellPrice - currentPrice) / sellPrice

Buyback Score = 
  (normalize(priceDropPercent) * 0.35) +
  (Volatility Index * 0.25) +
  (Hero Trend Score * 0.20) +
  (Demand Ratio * 0.10) +
  (Price Momentum * 0.05) +
  (Liquidity Score * 0.05)
```

**Результат:** Округляется до целого числа (0-100)

### Risk Level (Уровень риска)
**Формула:**
```
Если Investment/Buyback Score >= 70 и Volatility < 0.5 и Demand Ratio > 0.6:
  → 'Низкий'
Иначе если Investment/Buyback Score >= 50 и Volatility < 0.7 и Demand Ratio > 0.4:
  → 'Средний'
Иначе:
  → 'Высокий'
```

---

## 📋 ГЕНЕРАЦИЯ РЕЗУЛЬТАТОВ

### Столбцы в History
- **I**: Оценка инвестиции (🟢 85) - Investment Score (0-100)
- **J**: Рекомендация (🟩 КУПИТЬ, 🟨 ДЕРЖАТЬ, 🟥 ПРОДАТЬ, 👀 НАБЛЮДАТЬ)
- **K**: Фаза цикла (🟩 ДНО, 🟨 РОСТ, 🔥 ПИК, 🟥 СПАД)
- **L**: Потенциал (P85) - число (0.14 для +14%)
- **M**: Тренд - объединенный формат ("🟥 Падает 35 дн.")
- **N**: Тренд героя (🟢 85) - Hero Trend Score (0-100)

### Столбцы в Invest
- **N**: Оценка инвестиции (🟢 85) - Investment Score (0-100)
- **O**: Рекомендация (🟩 КУПИТЬ, 🟨 ДЕРЖАТЬ, 🟥 ПРОДАТЬ, 👀 НАБЛЮДАТЬ)
- **T**: Уровень риска (Низкий/Средний/Высокий)

### Столбцы в Sales
- **J**: Оценка откупа (🟢 85) - Buyback Score (0-100)
- **K**: Рекомендация (💰 ОТКУПИТЬ, 🟨 РАССМОТРЕТЬ, 🟥 НЕ ОТКУПАТЬ, 👀 НАБЛЮДАТЬ)
- **M**: Уровень риска (Низкий/Средний/Высокий)

### Логика рекомендаций

**Для Invest и History:**
- Investment Score >= 75 → 🟩 КУПИТЬ
- Investment Score >= 60 → 🟨 ДЕРЖАТЬ
- Investment Score < 40 → 🟥 ПРОДАТЬ
- Иначе → 👀 НАБЛЮДАТЬ

**Для Sales:**
- Buyback Score >= 75 → 💰 ОТКУПИТЬ
- Buyback Score >= 60 → 🟨 РАССМОТРЕТЬ
- Buyback Score < 40 → 🟥 НЕ ОТКУПАТЬ
- Иначе → 👀 НАБЛЮДАТЬ

---

## 📱 TELEGRAM УВЕДОМЛЕНИЯ

### Критические уведомления (немедленно)
- **Investment Score >= 75** из History (для предметов НЕ в портфеле)
- **Buyback Score >= 75** из Sales
- **Изменение цены > 20%** за 24 часа

### Важные уведомления
- **Изменение Hero Trend Score > 15%** за 24 часа (группируются топ-5)

### Ежедневные отчеты (13:00)
- Общие метрики портфеля
- Топ-5 возможностей из History (Investment Score >= 75)
- Топ-5 откупов из Sales (Buyback Score >= 75)

---

## 🔧 ПОРОГИ И КОНСТАНТЫ

### Пороги для критических сигналов
```javascript
INVESTMENT_SCORE_CRITICAL: 75  // 0-100 шкала
BUYBACK_SCORE_CRITICAL: 75     // 0-100 шкала
HERO_CHANGE_24H: 0.15          // 15% за 24 часа
PRICE_CHANGE_24H: 0.20         // 20% за 24 часа
```

### Веса для Investment Score
```javascript
HERO_TREND: 0.35
VOLATILITY: 0.20
DEMAND_RATIO: 0.20
PRICE_MOMENTUM: 0.15
LIQUIDITY: 0.05
SALES_TREND: 0.05
```

### Веса для Buyback Score
```javascript
PRICE_DROP: 0.35
VOLATILITY: 0.25
HERO_TREND: 0.20
DEMAND_RATIO: 0.10
PRICE_MOMENTUM: 0.05
LIQUIDITY: 0.05
```

### Веса для Hero Trend Score
```javascript
CONTEST_RATE_CHANGE: 0.40
PRO_CONTEST_RATE_CHANGE: 0.20
PICK_RATE: 0.25
BAN_RATE: 0.10
WIN_RATE: 0.05
```

---

## 📝 ПРИМЕЧАНИЯ

1. **Форматирование скоров:** `analytics_formatScore()` автоматически определяет формат (0-1 или 0-100) и нормализует для отображения
2. **Герои без про-статистики:** Вес `PRO_CONTEST_RATE_CHANGE` перераспределяется на `CONTEST_RATE_CHANGE`
3. **Common Items:** Используют модифицированную формулу без Hero Trend Score
4. **Корреляции:** Применяются только для Hero Items с Investment Score
5. **Валидация:** Все scores проверяются на диапазон 0-100 перед записью

