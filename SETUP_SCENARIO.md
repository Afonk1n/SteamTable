# ПОЛНЫЙ СЦЕНАРИЙ НАСТРОЙКИ НОВОЙ ТАБЛИЦЫ

## 📋 ОБЗОР

Этот документ описывает полный путь от пустой таблицы Google Sheets до полностью рабочей системы SteamTable со всем функционалом.

---

## 🚀 ЭТАПЫ НАСТРОЙКИ

### ЭТАП 0: Подготовка (вручную)

1. **Создание новой таблицы Google Sheets**
   - Создать новую таблицу в Google Sheets
   - Назвать её (например, "SteamTable")

2. **Копирование скриптов**
   - Открыть редактор скриптов (Расширения → Apps Script)
   - Скопировать все файлы `.gs` в проект
   - Сохранить проект

3. **Первый запуск**
   - Обновить страницу таблицы
   - Меню "SteamTable" появится автоматически (вызывается `onOpen()`)

---

### ЭТАП 1: Инициализация структуры таблиц

**Действие:** `SteamTable → Инициализировать все таблицы`

**Что делает:**
- Создает и форматирует все листы:
  - History
  - Invest
  - Sales
  - PortfolioStats
  - HeroStats
  - HeroMapping
  - AutoLog
  - Log

**Результат:** Все таблицы созданы и отформатированы, готовы к заполнению данными.

**Проверка:** Убедиться, что все листы созданы и имеют правильные заголовки.

---

### ЭТАП 2: Заполнение данных History

**Вариант A: Ручное заполнение**
- Добавить предметы в лист History (колонка B - Название)
- Можно добавить изображения и ссылки вручную или использовать функцию позже

**Вариант B: Импорт данных**
- Импортировать список предметов из другого источника
- Убедиться, что названия соответствуют названиям на Steam Market

**После заполнения:**
- `History → Изображения и ссылки` - обновить изображения и ссылки для всех предметов

**Результат:** Лист History содержит список предметов для отслеживания.

---

### ЭТАП 3: Первоначальная настройка данных

**Действие:** `SteamTable → Первоначальная настройка`

**Что делает (пошагово):**

1. **Шаг 1/4: Заполнение Min/Max цен**
   - Выбор: для всех предметов / только отсутствующих / пропустить
   - Выполняет: `priceHistory_calculateMinMaxForAllItems()`
   - Заполняет колонки G (Min) и H (Max) в History

2. **Шаг 2/4: Синхронизация HeroMapping**
   - Выбор: синхронизировать / пропустить
   - Выполняет: `heroMapping_syncWithHistory()`
   - Добавляет предметы из History в HeroMapping

3. **Шаг 3/4: Автоопределение героев**
   - Выбор: определить / пропустить
   - Выполняет: `heroMapping_autoDetectFromSteamWebAPI()`
   - Определяет героев для Hero Items через SteamWebAPI

4. **Шаг 4/4: Обновление статистики героев**
   - Выбор: обновить / пропустить
   - Выполняет: `heroStats_updateAllStats()`
   - Получает статистику всех героев через OpenDota API

**Результат:** Базовая структура данных заполнена.

**⚠️ ВАЖНО:** После этого этапа нужно обновить аналитику и метрики (см. Этап 4).

---

### ЭТАП 3/4: Полная настройка (РЕКОМЕНДУЕТСЯ)

**Действие:** `SteamTable → Полная настройка (все шаги)`

**Что делает:**
- Автоматически выполняет все необходимые шаги для полной настройки:
  1. Расчет Min/Max из SteamWebAPI (если отсутствуют)
  2. Настройка HeroMapping (синхронизация + автоопределение героев)
  3. Обновление статистики героев + синхронизация в History
  4. Обновление аналитики History и Investment Scores

**⚠️ УСЛОВИЯ:**
- Таблицы должны быть инициализированы (Этап 1)
- Предметы должны быть добавлены в History (Этап 2)

**Результат:** Система полностью настроена и готова к работе.

**Альтернатива:** Можно выполнить шаги по отдельности:
- `Шаг 0: Расчет Min/Max из SteamWebAPI`
- `Шаг 1: Настройка HeroMapping`
- `Шаг 2: Обновление статистики героев`
- `Шаг 3: Обновление аналитики и метрик`

---

### ЭТАП 3/4 (АЛЬТЕРНАТИВНЫЙ): Первоначальная настройка + ручное обновление аналитики

> **Примечание:** Этот вариант более длительный, рекомендуется использовать "Полная настройка" выше.

**Вариант A: `SteamTable → Первоначальная настройка`**
- Включает шаги с диалогами для каждого действия
- Требует ручного подтверждения на каждом шаге

**После `performInitialSetup()` нужно вручную:**
1. `History → Обновить аналитику`
2. `History → Синхронизировать статистику героев`
3. `History → Обновить Investment Scores`
4. `Синхронизация → Обновить все метрики и скоры`

---

### ЭТАП 5: Настройка автоматизации

1. **Включить автообновление**
   - `SteamTable → Включить автообновление`
   - Выполняет: `setupAllTriggers()`
   - Создает триггеры:
     - 00:00 - создание колонки "ночь" и начало сбора цен
     - 12:00 - создание колонки "день" и начало сбора цен
     - Каждые 10 минут - продолжение сбора цен
     - 13:00 - ежедневные Telegram уведомления (если настроен)

**Результат:** Система автоматически обновляет цены и аналитику.

---

### ЭТАП 6: Настройка Telegram (опционально)

1. **Настроить Telegram**
   - `Telegram → Настроить Telegram`
   - Выполняет: `telegram_setup()`
   - Вводит: Bot Token и Chat ID

2. **Тест подключения**
   - `Telegram → Тест Telegram`
   - Выполняет: `telegram_testConnection()`
   - Проверяет подключение к боту

3. **Тест уведомлений**
   - `Telegram → Тест ежедневных уведомлений`
   - Выполняет: `telegram_testDailyNotifications()`
   - Отправляет тестовые уведомления

**Результат:** Telegram уведомления настроены и работают.

---

### ЭТАП 7: Рабочий режим (полностью автоматический)

> **Цель:** После первого качественного запуска таблица функционирует полностью автоматически без ежедневного вмешательства пользователя.

**Полностью автоматические операции (работают по триггерам):**

1. **Сбор и обновление цен:**
   - 00:00 - создание колонки "ночь" и начало сбора цен
   - 12:00 - создание колонки "день" и начало сбора цен
   - Каждые 10 минут - продолжение сбора цен до завершения периода
   - После завершения сбора - автоматическое обновление аналитики History

2. **Синхронизация данных:**
   - Автоматическая синхронизация цен из History в Invest/Sales
   - Автоматическое обновление аналитики Invest/Sales (Min/Max, Тренд, Фаза/Потенциал/Рекомендация)
   - Автоматический расчет метрик Invest/Sales после синхронизации цен

3. **Обновление статистики героев:**
   - 08:00, 14:00, 20:00 - автоматическое обновление статистики героев через OpenDota API
   - После обновления - автоматическая синхронизация статистики в History
   - После синхронизации - автоматическое обновление Investment Scores и Buyback Scores

4. **Обновление метрик и скоров:**
   - Автоматическое обновление всех метрик после обновления статистики героев
   - Автоматическое обновление Investment Scores после обновления статистики
   - Автоматическое обновление Buyback Scores после обновления метрик

5. **Обслуживание данных:**
   - Раз в неделю (воскресенье 03:00) - автоматическая архивация старых данных HeroStats (старше 90 дней)
   - Раз в день (04:00) - автоматическая синхронизация HeroMapping с History и автоопределение героев для новых предметов

6. **Уведомления:**
   - 13:00 - ежедневные Telegram уведомления (если настроен Telegram)

**Минимальные ручные операции (только при необходимости):**

1. **Добавление новых предметов:**
   - Добавление предметов в History (вручную)
   - HeroMapping и статистика обновятся автоматически

2. **Управление портфелем:**
   - Отметка чекбоксов "Купить?" в History → автоматическое добавление в Invest
   - Отметка чекбоксов "Продать?" в Invest → автоматическое добавление в Sales

3. **Ручное вмешательство (только при проблемах):**
   - При необходимости можно вручную обновить метрики или скоры
   - При необходимости можно вручную обновить статистику героев
   - Все операции логируются в AutoLog для отслеживания

**Результат:** Система работает полностью автоматически, пользователь только добавляет новые предметы и управляет портфелем через чекбоксы.

---

## 🔍 ПРОВЕРКА СООТВЕТСТВИЯ ФУНКЦИЙ В МЕНЮ

### ✅ Все функции из меню существуют:

- [x] `setupAllTriggers` - Menu.gs
- [x] `removeAllTriggers` - Menu.gs
- [x] `initializeAllTables` - Menu.gs
- [x] `performInitialSetup` - Menu.gs
- [x] `history_updateAllPrices` - History.gs
- [x] `priceHistory_calculateMinMaxForAllItems` - PriceHistoryUtils.gs
- [x] `priceHistory_calculateMinMaxForMissingItems` - PriceHistoryUtils.gs
- [x] `invest_formatTable` - Invest.gs
- [x] `invest_updateImagesAndLinks` - Invest.gs
- [x] `invest_findDuplicates` - Invest.gs
- [x] `invest_calculateAllMetrics` - Invest.gs
- [x] `invest_updateInvestmentScores` - Invest.gs
- [x] `sales_formatTable` - Sales.gs
- [x] `sales_updateImagesAndLinks` - Sales.gs
- [x] `sales_findDuplicates` - Sales.gs
- [x] `sales_calculateAllMetrics` - Sales.gs
- [x] `sales_updateBuybackScores` - Sales.gs
- [x] `history_formatTable` - History.gs
- [x] `history_updateAllAnalytics_` - History.gs
- [x] `history_updateImagesAndLinks` - History.gs
- [x] `history_findDuplicates` - History.gs
- [x] `history_ensureTodayColumn` - History.gs
- [x] `history_syncHeroStats` - History.gs
- [x] `history_updateInvestmentScores` - History.gs
- [x] `portfolioStats_formatTable` - PortfolioStats.gs
- [x] `portfolioStats_saveHistoryManual` - PortfolioStats.gs
- [x] `heroStats_formatTable` - HeroStats.gs
- [x] `heroStats_updateAllStats` - HeroStats.gs
- [x] `heroStats_archiveOldData` - HeroStats.gs
- [x] `heroMapping_formatTable` - HeroMapping.gs
- [x] `heroMapping_autoDetectFromSteamWebAPI` - HeroMapping.gs
- [x] `heroMapping_syncWithHistory` - HeroMapping.gs
- [x] `syncPricesFromHistoryToInvestAndSales` - Utils.gs
- [x] `syncAnalyticsForInvestSales_` - Menu.gs
- [x] `updateAllMetricsForInvestSales` - Utils.gs
- [x] `updateAllMetricsAndScores_` - Menu.gs
- [x] `openDota_testConnection` - OpenDotaAPI.gs
- [x] `steamWebAPI_testConnection` - SteamWebAPI.gs
- [x] `telegram_setup` - Telegram.gs
- [x] `telegram_testConnection` - Telegram.gs
- [x] `telegram_testDailyNotifications` - Telegram.gs

---

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

## ✅ РЕШЕННЫЕ УЛУЧШЕНИЯ

1. ✅ **Создана функция "Полная настройка"** (`performFullSetup`)
   - Объединяет все необходимые шаги: Min/Max, HeroMapping, статистика героев, аналитика и метрики
   - Один вызов для полной настройки без диалогов

2. ✅ **Добавлена проверка готовности** (`checkSystemReadiness`)
   - Проверяет наличие данных, заполненность Min/Max, наличие HeroMapping и HeroStats
   - Показывает, что еще нужно сделать

3. ✅ **Добавлена проверка автоматизации** (`checkAutomationStatus`)
   - Показывает активные триггеры и последние выполнения операций

## 📋 КРАТКИЙ СЦЕНАРИЙ НАСТРОЙКИ (МИНИМАЛЬНЫЙ ПУТЬ)

1. **Инициализация:** `SteamTable → Инициализировать все таблицы`
2. **Добавление предметов:** Вручную добавить предметы в History (колонка B)
3. **Полная настройка:** `SteamTable → Полная настройка (все шаги)`
4. **Включение автоматизации:** `SteamTable → Включить автообновление`
5. **Опционально:** Настроить Telegram для уведомлений

**Готово!** Система работает полностью автоматически.

