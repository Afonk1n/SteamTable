# ДОКУМЕНТАЦИЯ ПРОЕКТА SteamTable

> **Назначение**: Этот документ предназначен для AI агентов и содержит актуальную техническую информацию о структуре проекта, функциях, логике работы и важных деталях реализации.

> **Сценарий настройки**: Полный сценарий настройки новой таблицы от пустой до полностью рабочей описан в `SETUP_SCENARIO.md`.

---

## ПРАВИЛА РАБОТЫ С ДОКУМЕНТАЦИЕЙ

### Для AI агентов:

1. **Перед выполнением задачи**: Обязательно прочитай этот документ для синхронизации контекста
2. **После изменений в коде**: Актуализируй документацию - обнови описание функций, добавь важные технические детали, измени структуру если нужно
3. **Не хранить**: Историю выполненных задач, даты изменений, информацию о том "что было реализовано"
4. **Хранить**: Текущую структуру проекта, актуальное описание функций, технические детали реализации, известные проблемы и их решения

---

## СТРУКТУРА ПРОЕКТА

### Файлы кода:

1. **Constants.gs** - Константы и конфигурация (STEAM_FEE, COLORS, NUMBER_FORMATS, TREND_ANALYSIS_CONFIG, API_CONFIG, HERO_STATS, HERO_MAPPING, ANALYTICS_WEIGHTS, ANALYTICS_THRESHOLDS)
2. **SheetService.gs** - Утилиты для работы с листами (`getOrCreateSheet_`, `formatHeaderRange_`, `createLogSheet_`, `getHeroStatsSheet_`, `getHeroMappingSheet_`)
3. **History.gs** - Основная логика листа History (цены, тренды, аналитика, Investment Score, статистика героев)
4. **Invest.gs** - Логика листа Invest (портфель инвестиций, расчет прибыли, Investment Score, метрики)
5. **Sales.gs** - Логика листа Sales (проданные предметы, расчет просадки, Buyback Score, метрики)
6. **Analytics.gs** - Расчет экономических метрик и скоров (Liquidity Score, Demand Ratio, Price Momentum, Sales Trend, Volatility Index, Hero Trend Score, Investment Score, Buyback Score, Risk Level)
7. **Utils.gs** - Унифицированная система обновления цен, синхронизация, блокировки
8. **Menu.gs** - Меню и триггеры (onOpen, setupAllTriggers, performInitialSetup)
9. **EventHandler.gs** - Обработка событий листов (чекбокс "Купить" в History и "Продать" в Invest)
10. **FormattingService.gs** - Утилиты форматирования (универсальные функции для всех таблиц)
11. **Calculations.gs** - Batch-расчеты прибыли/убытка для Invest/Sales
12. **PortfolioStats.gs** - Трекинг истории портфеля (сохранение метрик)
13. **Telegram.gs** - Интеграция с Telegram Bot API (уведомления, отчеты)
14. **OpenDotaAPI.gs** - Интеграция с OpenDota API для статистики героев (пикрейт, винрейт, банрейт, про-статистика)
15. **SteamWebAPI.gs** - Интеграция с SteamWebAPI.ru для получения данных о предметах (цены, теги, изображения, метрики) + утилиты для расчета Min/Max цен (объединены из PriceHistoryUtils.gs)
16. **HeroStats.gs** - Управление листом HeroStats (статистика героев по датам, включая про-статистику)
17. **HeroMapping.gs** - Управление листом HeroMapping (связь предметов с героями)

### Листы таблицы:

- **History** - Основной лист с историей цен по датам
- **Invest** - Портфель активных инвестиций
- **Sales** - Проданные предметы (отслеживание просадки цены)
- **PortfolioStats** - История метрик портфеля (динамика во времени)
- **HeroStats** - Статистика героев по датам (пикрейт, винрейт, банрейт, contest rate)
- **HeroMapping** - Маппинг предметов на героев (с изображениями и автоопределением)
- **AutoLog** - Лог автоматических действий скрипта
- **Log** - Лог операций покупки/продажи
- **TelegramNotifications** - История отправленных Telegram уведомлений (для анализа и отслеживания)
  - Структура: Дата/Время, Тип, Предмет, Событие, Данные (JSON), Приоритет
  - Создается автоматически при первом использовании системы уведомлений

---

## КРИТИЧЕСКИ ВАЖНЫЕ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Группировка цен по дате

**ОБЯЗАТЕЛЬНО**: Все функции, работающие с историей цен, должны группировать цены по дате (игнорируя период "ночь"/"день").

**Почему**: Период "ночь" (00:10-12:00) и "день" (12:00-00:10) относятся к одной календарной дате. Для анализа трендов, фазы цикла и потенциала нужна одна цена на день.

**Как группировать**:
1. Собрать все цены с их датами из заголовков колонок
2. Парсить дату из заголовка (формат: "dd.MM.yy ночь" или "dd.MM.yy день")
3. Группировать по ключу `dd.MM.yy`, беря **последнюю цену** за день
4. Отсортировать даты в хронологическом порядке

**Где используется**:
- `history_analyzeTrend()` - анализ тренда
- `history_updateTrends()` - расширенная аналитика (фаза, потенциал)
- `history_calculateDaysChange_()` - расчет дней смены тренда

**Важно для расчета дней**:
- Уникальный список дат создается из Map (без дубликатов) - гарантирует одну запись на день
- При создании объектов Date используется полдень (12:00:00) для избежания проблем с часовыми поясами
- Для расчета разницы в днях используется `Math.floor` (точный подсчет календарных дней, не округление вверх)
- Разница вычисляется между реальными датами, а не количеством записей

### Формат данных для потенциала

**Потенциал хранится как число**, не строка:
- Число: `0.14` (для +14%)
- Форматирование: `'+0%;-0%;"—"'` для отображения "+14%" или "-5%"
- Это позволяет сортировку и правильные математические операции

**Файлы**: `History.gs`, `Invest.gs`, `Sales.gs`, `Utils.gs`

### Порядок применения форматирования

**ОБЯЗАТЕЛЬНЫЙ порядок**:
1. Сначала условное форматирование (`history_applyAllConditionalFormatting_`)
2. Потом прямое форматирование - выделение min/max (`history_highlightMinMax_`)

**Почему**: Прямое форматирование перезаписывает условное. Если порядок нарушен, min/max не будут выделены.

**Файл**: `History.gs`, функция `history_updateAllAnalytics_()`

### Обработка ошибок при создании колонок периодов

**Проблема**: Триггер настроен на 00:00, но проверка времени требовала 00:10, что вызывало ошибки.

**Решение**:
- Разрешено создание колонки "ночь" с 00:00 до 12:00 (вместо строго 00:10)
- `history_updatePricesForPeriod` возвращает `false` вместо выброса ошибки, если колонка не создана
- `unified_priceUpdate` корректно обрабатывает `false` - просто выходит без ошибки

**Файлы**: `History.gs`, `Utils.gs`

### Вставка новых записей

**Все новые записи вставляются сверху** (после заголовка):
- Используется `sheet.insertRowAfter(HEADER_ROW)` 
- Применяется для: логов (AutoLog, Log), истории PortfolioStats

**Файлы**: `Utils.gs`, `PortfolioStats.gs`

### Валидация цен перед записью

**ОБЯЗАТЕЛЬНО**: Все цены перед записью должны проходить валидацию через `validatePrice_()`.

**Проверки**:
- Тип данных должен быть `number`
- Значение должно быть конечным числом (`Number.isFinite()`)
- Цена должна быть положительной (> 0)
- Цена не должна превышать `MAX_REASONABLE_PRICE` (50000 ₽)

**Использование**:
- `history_updatePricesForPeriod()` - валидация перед записью цен в колонки дат
- `history_updateSinglePrice()` - валидация при ручном обновлении цены
- `history_normalizePriceFormats()` - валидация при нормализации форматов цен

**Файлы**: `Utils.gs` (функция `validatePrice_()`), `History.gs`, `Constants.gs` (константа `MAX_REASONABLE_PRICE`)

### Валидация статистики героев

**Расчет изменения contestRateChange7d**:
- Используется `contestRatePercent` вместо абсолютного `contestRate` для предотвращения аномальных значений
- Изменения ограничиваются диапазоном от -1000% до +1000%
- При превышении ограничений значение обрезается до максимума/минимума с логированием предупреждения

**Файлы**: `HeroStats.gs` (функция `heroStats_updateAllStats()`)

### Обработка ошибок и таймауты в Invest

**Все циклы обработки данных в Invest имеют защиту**:
- Таймаут выполнения: максимум 5 минут (300000 мс) на функцию
- Try-catch блоки для каждой итерации цикла
- При ошибке для строки - добавляются null значения вместо зависания
- Валидация Investment Score перед записью (проверка диапазона 0-100)

**Файлы**: `Invest.gs` (функции `invest_calculateAllMetrics()`, `invest_updateInvestmentScores()`)

### Структура Sales (без колонки Количество)

**Sales хранит историю проданных предметов для потенциального откупа**:
- Колонка "Количество" удалена - не нужна для отслеживания проданных предметов
- Структура: Изображение | Название | Цена продажи | Текущая цена | Просадка | Просадка % | Ссылка | Min | Max | Оценка откупа | Рекомендация | Тренд героя | Уровень риска
- При продаже из Invest через чекбокс "Продать":
  - Если предмет найден в Sales: обновляется цена продажи только если новая цена больше текущей (хранится максимальная)
  - Если предмет не найден: добавляется новая строка с текущей ценой продажи

**Файлы**: `Sales.gs`, `Constants.gs` (SALES_COLUMNS, HEADERS.SALES), `Invest.gs` (EventHandler)

### Форматирование Telegram уведомлений

**Формат названия предмета**:
- Включает смайлик по Investment Score (🟢 >=75, 🟡 >=60, ⚪ >=40, 🔴 <40)
- Включает значение Investment Score рядом с названием (0-100 шкала)
- Формат: `Предмет: <b>Название</b> 🟡 72` (смайлик и скор после названия)

**Формат просадки**:
- Используется "(макс)" вместо "цель" для указания максимальной цены продажи
- Формат: `Текущая цена: 15,00 ₽ (макс: 30,00 ₽)`

**Убраны лишние элементы**:
- Удалена строка "Рекомендация: 🟩 КУПИТЬ" внизу сообщений

**Файлы**: `Telegram.gs` (функции `telegram_formatItemNameWithScore_()`, `telegram_getScoreEmoji_()`, обновлены все функции отправки уведомлений)

### Защита от зависаний в History и Sales

**History (history_updateTrends)**:
- Таймаут выполнения: максимум 5 минут на всю функцию
- Таймаут обработки строки: максимум 5 секунд на одну строку
- Валидация цен перед анализом (проверка через `validatePrice_`)
- Проверка достаточности данных перед анализом (минимум 2 цены)
- Try-catch блоки для обработки ошибок

**Sales (sales_calculateAllMetrics, sales_updateBuybackScores)**:
- Таймаут выполнения: максимум 5 минут на функцию
- Валидация цен перед использованием (проверка через `validatePrice_`)
- Try-catch блоки для каждой строки
- Логирование ошибок и пропущенных данных
- Валидация Buyback Score перед записью (проверка диапазона 0-100)

**Файлы**: `History.gs` (функция `history_updateTrends()`), `Sales.gs` (функции `sales_calculateAllMetrics()`, `sales_updateBuybackScores()`)

---

## ЛОГИКА РАБОТЫ СИСТЕМЫ

### Поток данных

```
Steam Market API
    ↓
History (сохранение цен по датам)
    ↓
Синхронизация цен в Invest/Sales (syncPricesFromHistoryToInvestAndSales)
    ↓
Расчет аналитики в History (тренды, фазы, потенциал)
    ↓
Синхронизация аналитики в Invest/Sales
    ↓
Сохранение истории портфеля (PortfolioStats, только после периода "день")
```

### Автоматическое обновление (триггеры)

**Архитектура**:
1. **00:00** - `history_createPeriodAndUpdate_morning()` создает колонку периода "ночь" и начинает сбор цен
2. **12:00** - `history_createPeriodAndUpdate_evening()` создает колонку периода "день" и начинает сбор цен
3. **Каждые 10 минут** - `unified_priceUpdate()` продолжает сбор цен до завершения периода
4. **13:00** - `telegram_checkDailyPriceTargets()` отправляет ежедневные уведомления (если настроен Telegram)
5. **08:00, 14:00, 20:00** - `autoUpdateHeroStats()` автоматическое обновление статистики героев и скоров
6. **04:00** - `autoSyncHeroMapping()` автоматическая синхронизация HeroMapping с History
7. **Воскресенье 03:00** - `autoArchiveHeroStats()` автоматическая архивация старых данных HeroStats (старше 90 дней) и очистка старых Telegram уведомлений

**Автоматические Telegram уведомления (если настроен Telegram):**
- Немедленно после обновления Investment Scores - проверка возможностей для покупки из History (Investment Score >= 75)
- Немедленно после обновления Buyback Scores - проверка возможностей для откупа из Sales (Buyback Score >= 75)
- Немедленно после обновления цен - проверка резких изменений цены (>20% за 24ч)
- После синхронизации статистики героев - проверка изменений Hero Trend Score (>15% за 24ч, группируются топ-5)

**Периоды сбора цен**:
- **"ночь"**: 00:10 - 12:00 (техническое название для периода 00:10-12:00)
- **"день"**: 12:00 - 00:10 следующего дня (техническое название для периода 12:00-00:10)

**Завершение периода**:
Когда сбор цен завершен (`historyCompleted === true`):
1. Обновляется аналитика в History (тренды, фазы, min/max)
2. Синхронизируется аналитика в Invest/Sales
3. Если период === "день" → сохраняется история в PortfolioStats и обновляются Investment/Buyback Scores

**Важно**: Аналитика Invest/Sales обновляется автоматически внутри `syncPricesFromHistoryToInvestAndSales()`, поэтому при завершении периода вызывается только сохранение истории портфеля. После завершения дневного сбора автоматически обновляются Investment Scores для History и Invest, а также Buyback Scores для Sales. После обновления скоров автоматически проверяются возможности для покупки/откупа и отправляются критические уведомления в Telegram (если настроен).

**Автоматическое обновление статистики героев**:
Когда `autoUpdateHeroStats()` выполняется (3 раза в день):
1. Обновляется статистика героев через OpenDota API (`heroStats_updateAllStats()`)
2. Синхронизируется статистика в History (`history_syncHeroStats()`)
3. Проверяются изменения Hero Trend Score и отправляются важные уведомления (если настроен Telegram)
4. Обновляются Investment Scores для History и Invest (`history_updateInvestmentScores()`, `invest_updateInvestmentScores()`)
5. Обновляются Buyback Scores для Sales (`sales_updateBuybackScores()`)
6. Автоматически проверяются возможности для покупки/откупа и отправляются критические уведомления в Telegram (если настроен)

**Автоматическая синхронизация HeroMapping**:
Когда `autoSyncHeroMapping()` выполняется (ежедневно в 04:00):
1. Синхронизируются предметы из History с HeroMapping (`heroMapping_syncWithHistory()`)
2. Автоматически определяются герои для новых предметов (`heroMapping_autoDetectFromSteamWebAPI()`)
3. Автоматически заполняются пустые Hero ID из HeroStats (`heroMapping_fillMissingHeroIds()`)

**Автоматическая архивация данных**:
Когда `autoArchiveHeroStats()` выполняется (воскресенье в 03:00):
1. Удаляются данные старше HERO_STATS_HISTORY_DAYS (90 дней) из HeroStats (`heroStats_archiveOldData()`)

### Синхронизация данных

**Источник данных - History**:
- **Min/Max**: Из всех цен в колонках дат (поиск минимума/максимума)
- **Тренд**: Из колонки M (объединенный формат "🟥 Падает 35 дн.") History
- **Фаза/Потенциал/Рекомендация**: Из колонок K, L, J History

**Функции синхронизации**:
- `invest_syncMinMaxFromHistory()` / `sales_syncMinMaxFromHistory()`
- `invest_syncTrendDaysFromHistory()` / `sales_syncTrendDaysFromHistory()`
- `invest_syncExtendedAnalyticsFromHistory()` / `sales_syncExtendedAnalyticsFromHistory()`

---

## СТРУКТУРА КОЛОНОК

### History (основной лист)

- **A**: Изображение
- **B**: Название предмета
- **C**: Статус (✓, ❌, ⚠️, 🆕, ❓)
- **D**: Ссылка на Steam Market
- **E**: Чекбокс "Купить"
- **F**: Текущая цена
- **G**: Min (минимальная цена за историю)
- **H**: Max (максимальная цена за историю)
- **I**: Оценка инвестиции (🟢 85) - итоговая оценка для покупки (0-100 шкала)
- **J**: Рекомендация (текстовая рекомендация: 🟩 КУПИТЬ, 🟨 ДЕРЖАТЬ, 🟥 ПРОДАТЬ, 👀 НАБЛЮДАТЬ)
- **K**: Фаза цикла (Дно, Рост, Пик, Спад)
- **L**: Потенциал (P85) - **число** (0.14 для +14%), форматирование '+0%;-0%;"—"'
- **M**: Тренд - **объединенный формат** ("🟥 Падает 35 дн." или "🟩 Растет 12 дн." или "🟨 Боковик 39 д.")
  - Объединяет направление тренда и количество дней с последней смены
- **N**: Тренд героя (🟢 85) - тренд героя (фундаментальная оценка популярности и мета-значимости, 0-100 шкала)
  - Учитывает недельные изменения (7d), про-мету, текущую популярность Иммортал, Win Rate
- **O**: Контест-рейт про-сцены - процент контест-рейта про-сцены (%)
  - **Что это:** Процент про-матчей, в которых герой был выбран (proPick) или забанен (proBan)
  - **Расчет:** (proPick + proBan) / общее количество про-матчей * 100
  - **Важно:** Показывает актуальную про-мету (водопадная мета начинается здесь)
- **P**: Изменение контест-рейта про (7д) - изменение про-контест-рейта за неделю (%)
  - Положительное значение = герой становится популярнее на про-сцене
  - Отрицательное значение = герой теряет популярность на про-сцене
- **Q**: Изменение пик-рейта Иммортал (7д) - изменение пикрейта Иммортал за неделю (%)
  - Положительное значение = герой становится популярнее на Иммортал
  - Отрицательное значение = герой теряет популярность на Иммортал
- **R**: Изменение пик-рейта Иммортал (24ч) - изменение пикрейта Иммортал за 24 часа (%)
  - **Важно:** Главный индикатор патч-имб (быстрая реакция на изменения)
  - Положительное значение = резкий рост популярности (возможная имба после патча)
- **S**: Пик-рейт Иммортал - текущий пикрейт Иммортал (%)
  - **Что это:** Процент матчей Иммортал, в которых герой был выбран
  - **Ранг:** Только Иммортал (ранг 8) - более точная метрика для инвестиций
  - **Пример:** 1,4% = герой был выбран в 1,4% всех матчей Иммортал
- **T**: Винрейт - текущий винрейт (%)
  - **Что это:** Процент побед героя в матчах
  - **Пример:** 52,02% = герой выигрывает в 52% матчей
- **U**: Имя героя - имя героя (из HeroMapping)
- **V**: Мета сигнал (🔥 92) - краткосрочный индикатор патч-имб (0-100 шкала)
  - **Что это:** Отдельный параметр для быстрых изменений меты (24h изменения, патч-имбы)
  - **Отличие от Hero Trend:** Hero Trend - фундаментальная оценка (недельные тренды), Мета сигнал - быстрый индикатор (24h)
  - **Использование:** Во время патча ориентироваться по этому столбцу для быстрых решений
  - **Формат:** 🔥 >=75 (горячий сигнал), 🟡 >=60, ⚪ >=40, 🔴 <40
  - **Обычно низкий, во время патча повышается**
- **W+**: Колонки с датами (формат заголовка: "dd.MM.yy ночь" или "dd.MM.yy день")

### Invest (портфель инвестиций)

- **A**: Изображение
- **B**: Название
- **C**: Количество
- **D**: Цена покупки (усредненная при добавлении)
- **E**: Текущая цена (синхронизируется из History)
- **F**: Цель (целевая цена продажи)
- **G**: Общие вложения (количество × цена покупки)
- **H**: Текущая стоимость с комиссией (количество × текущая цена × 0.85)
- **I**: Профит в рублях (текущая стоимость с комиссией - вложения)
- **J**: Прибыль % с комиссией ((текущая стоимость с комиссией / вложения) - 1)
- **K**: Ссылка
- **L**: Min (из History)
- **M**: Max (из History)
- **N**: Оценка инвестиции (🟢 85) - итоговая оценка для покупки (0-100 шкала)
- **O**: Рекомендация (🟩 КУПИТЬ, 🟨 ДЕРЖАТЬ, 🟥 ПРОДАТЬ, 👀 НАБЛЮДАТЬ)
- **P**: Фаза (из History)
- **Q**: Потенциал (P85) - **число** (из History)
- **R**: Тренд (из History, объединенный формат "🟥 Падает 35 дн." или "🟨 Боковик 39 д.")
  - Объединяет направление тренда и количество дней с последней смены
- **S**: Тренд героя (🟢 85) - тренд героя (фундаментальная оценка, 0-100 шкала)
- **T**: Уровень риска (Низкий/Средний/Высокий) - уровень риска
- **U**: Чекбокс "Продать"
- **V**: Мета сигнал (🔥 92) - краткосрочный индикатор патч-имб (0-100 шкала)
  - **Что это:** Отдельный параметр для быстрых изменений меты (24h изменения, патч-имбы)
  - **Использование:** Во время патча ориентироваться по этому столбцу для быстрых решений

**Примечание:** Метрики (Volatility Index, Demand Ratio, Price Momentum, Sales Trend, Liquidity Score) удалены из отображения, но остаются в коде для расчета Investment Score и Risk Level.

### Sales (проданные предметы)

- **A**: Изображение
- **B**: Название
- **C**: Цена продажи (максимальная, если продано несколько раз)
- **D**: Текущая цена (синхронизируется из History)
- **E**: Просадка (цена продажи - текущая цена)
- **F**: Процент просадки ((цена продажи - текущая цена) / цена продажи)
- **G**: Ссылка
- **H**: Min (из History)
- **I**: Max (из History)
- **J**: Оценка откупа (🟢 85) - итоговая оценка для откупа (0-100 шкала)
- **K**: Рекомендация (💰 ОТКУПИТЬ, 🟨 РАССМОТРЕТЬ, 🟥 НЕ ОТКУПАТЬ, 👀 НАБЛЮДАТЬ)
- **L**: Тренд героя (🟢 85) - тренд героя (фундаментальная оценка, 0-100 шкала)
- **M**: Уровень риска (Низкий/Средний/Высокий) - уровень риска
- **N**: Контест-рейт про-сцены - процент контест-рейта про-сцены (%)
- **O**: Изменение контест-рейта про (7д) - изменение про-контест-рейта за неделю (%)
- **P**: Изменение пик-рейта Иммортал (24ч) - изменение пикрейта Иммортал за 24 часа (%)
  - **Важно:** Главный индикатор патч-имб (быстрая реакция на изменения)
- **Q**: Мета сигнал (🔥 92) - краткосрочный индикатор патч-имб (0-100 шкала)
  - **Что это:** Отдельный параметр для быстрых изменений меты (24h изменения, патч-имбы)
  - **Использование:** Во время патча ориентироваться по этому столбцу для быстрых решений

**Примечание:** Метрики (Volatility Index, Demand Ratio, Price Momentum, Sales Trend, Liquidity Score) удалены из отображения, но остаются в коде для расчета Buyback Score и Risk Level.

### PortfolioStats (история портфеля)

- **A**: Дата/Время (формат: 'dd.MM.yyyy HH:mm')
- **B**: Вложения (сумма всех вложений в активные позиции)
- **C**: Текущая стоимость (сумма текущей стоимости всех позиций с учетом комиссии)
- **D**: Прибыль/убыток в рублях
- **E**: Прибыль/убыток % (общий процент от вложений)
- **F**: Средняя прибыльность (средняя прибыльность всех позиций)

**Особенности**:
- Новые записи вставляются сверху (после заголовка)
- Сохранение происходит автоматически 1 раз в сутки после завершения периода "день"
- Есть ручная функция для тестирования (`portfolioStats_saveHistoryManual()`)

### HeroStats (статистика героев)

**Фиксированные колонки (A-C):**
- **A**: Hero ID (число, уникальный идентификатор героя)
- **B**: Hero Name (текст, имя героя)
- **C**: Rank Category (текст: "High Rank" / "All Ranks")

**Динамические колонки (D+):**
- Заголовок: `"DD.MM.YY HH:MM"` (дата и время обновления)
- Данные: JSON строка с статистикой `{pickRate, winRate, banRate, contestRate, matchCount, proPick, proBan, proContestRate, contestRateChange7d, proContestRateChange7d}`

**Особенности**:
- Две строки для каждого героя: одна для "High Rank", вторая для "All Ranks"
- Данные хранятся в JSON формате для компактности
- Включает про-статистику (`proPick`, `proBan`, `proContestRate`) из OpenDota API
- Автоматический расчет изменений за 7 дней (`contestRateChange7d`, `proContestRateChange7d`)
- Автоматическое архивирование данных старше 90 дней

### HeroMapping (маппинг предметов на героев)

- **A**: Item Name (текст, название предмета из History)
- **B**: Image (изображение предмета, формула `=IMAGE(url)`, автоматически заполняется при автоопределении)
- **C**: Hero Name (текст, имя героя, определяется автоматически через SteamWebAPI.ru)
- **D**: Hero ID (число, идентификатор героя)
- **E**: Category (текст: "Hero Item" / "Common Item")

**Особенности**:
- Автоматическое определение героев через SteamWebAPI.ru (tag5)
- Fallback на `/api/item_by_nameid` для предметов, не найденных через основной endpoint
- Автоматическое заполнение Hero ID из HeroStats по Hero Name (выполняется в `setupHeroStats()` и `autoSyncHeroMapping()`)
- Batch операции для быстрой обработки
- Синхронизация с History (добавляет только новые предметы)
- Полностью автономная система без ручной верификации (верификация убрана для упрощения)

---

## ВАЖНЫЕ ФУНКЦИИ И ИХ НАЗНАЧЕНИЕ

### History

- `history_analyzeTrend(row)` - Анализ тренда для одной строки (4 метода с голосованием, с группировкой по дате)
  - Создает уникальный список дат из Map (ночь и день одной даты = одна запись)
  - Использует полдень (12:00:00) при создании объектов Date для точности расчета
- `history_updateTrends()` - Обновление трендов для всех строк (оптимизированная версия с batch-чтением)
  - Создает уникальный список дат из Map (ночь и день одной даты = одна запись)
  - Использует полдень (12:00:00) при создании объектов Date для точности расчета
- `history_updateAllAnalytics_()` - Полное обновление аналитики: тренды (объединенный формат), фазы, потенциал, рекомендации, min/max, синхронизация статистики героев
- `history_syncHeroStats()` - Синхронизация статистики героев из HeroStats в History (колонки N-S)
- `history_updateHeroStatsColumns()` - Обновление колонок статистики героев (алиас для `history_syncHeroStats()`)
- `history_updateInvestmentScores()` - Расчет Investment Score для всех предметов в History
- `history_getPriceHistoryForItem_(sheet, row)` - Получение истории цен для предмета из History
- `history_updateCurrentPriceMinMax_(sheet)` - Обновление столбцов Текущая цена, Min, Max
  - Окрашивает устаревшие цены в желтый (COLORS.STABLE), если цена не за текущий период
  - Вызывается сразу после создания новой колонки периода для визуализации устаревших цен
  - **Логика Min/Max:** Min/Max получаются из SteamWebAPI один раз при первоначальной настройке. При обновлении цен проверяет: если новая цена < Min или > Max - обновляет только тогда. Не перезаписывает Min/Max из колонок дат каждый раз.
  - Fallback: если Min/Max не установлены из SteamWebAPI, вычисляет из существующих цен в колонках
- `history_calculateDaysChange_(prices, dates, currentTrend)` - Расчет дней смены тренда (используется внутри для объединенного формата)
  - Использует разницу между реальными датами (Math.floor), а не количество записей
  - Требует, чтобы dates были уже сгруппированы по дате (одна запись на день)
  - Использует полдень (12:00:00) в датах для точности расчета
  - Результат объединяется с трендом в колонке M через `history_formatDaysChange_()`
- `history_highlightMinMax_(sheet)` - Выделение ячеек с минимальными и максимальными ценами
  - Сбрасывает все фоны перед применением выделения, чтобы избежать наследования форматирования
- `history_applyAllConditionalFormatting_(sheet)` - Применение условного форматирования для трендов
- `history_ensurePeriodColumn(period)` - Создание колонки для периода (с проверкой времени)
  - Сбрасывает фон для всех ячеек новой колонки при создании, чтобы предотвратить наследование форматирования от предыдущих колонок
  - Возвращает номер созданной или существующей колонки
- `history_updatePricesForPeriod(period)` - Обновление цен для текущего периода
- `history_formatTable()` - Форматирование листа History
  - Форматирует колонки статистики героев: O (Изменение контест-рейта) - процент, P (Контест-рейт) - число, Q (Пик-рейт) - число, R (Винрейт) - процент
- `history_updateImagesAndLinks()` - Обновление изображений и ссылок из Steam API
- `history_normalizePriceFormats()` - Нормализация формата цен во всех колонках с датами
  - Конвертирует текстовые значения вида "39,99 ₽" в числа (например, 39.99)
  - Применяет единый формат `#,##0.00 ₽` ко всем колонкам с датами
  - Проверяет `displayValue` на наличие запятой/знака рубля даже если `value` уже число
  - Результат: все цены в том же формате, что и от SteamWebAPI (числа, а не текст)
  - Визуально значения выглядят так же благодаря формату валюты, но теперь их можно использовать в формулах

### Invest

- `invest_calculateBatch_(sheet, currentPrices)` - Batch-расчет прибыли для всех строк
- `invest_calculateSingle_(sheet, row, currentPrice)` - Расчет прибыли для одной строки
- `invest_syncMinMaxFromHistory()` - Синхронизация Min/Max из History
- `invest_syncTrendDaysFromHistory()` - Синхронизация Тренд (объединенный формат) из History
- `invest_syncExtendedAnalyticsFromHistory()` - Синхронизация Фаза/Потенциал/Рекомендация из History
- `invest_updateMetricsFromSteamWebAPI(itemNames)` - Получение данных из SteamWebAPI для расчета метрик
- `invest_calculateAllMetrics()` - Расчет всех метрик для позиций в Invest (Liquidity Score, Demand Ratio, Price Momentum, Sales Trend, Volatility Index, Hero Trend)
  - **Важно:** Метрики рассчитываются, но не записываются в таблицу (удалены из отображения)
  - Используются только для расчета Investment Score и Risk Level
- `invest_updateInvestmentScores()` - Расчет Investment Score для всех позиций в Invest
- `invest_generateRecommendation(row)` - Генерация рекомендации на основе Investment Score
- `invest_dailyReset()` - Очистка цен и пересчетов (вызывается при создании колонки периода)
  - Очищает текущие цены и расчеты
  - Синхронизация аналитики НЕ выполняется здесь (выполняется в `syncPricesFromHistoryToInvestAndSales()`)
- `invest_formatTable()` - Форматирование листа Invest
- `invest_formatGoalColumn_(startRow, endRow)` - Форматирование колонки "Цель" (красный/зеленый в зависимости от текущей цены)

### Sales

- `sales_calculateBatch_(sheet, currentPrices, sellPrices)` - Batch-расчет просадки цены
- `sales_calculateSingle_(sales, row, currentPrice)` - Расчет просадки для одной строки
- `sales_syncMinMaxFromHistory()` - Синхронизация Min/Max
- `sales_syncTrendDaysFromHistory()` - Синхронизация Тренд (не используется, т.к. в Sales нет колонки тренда)
- `sales_syncExtendedAnalyticsFromHistory()` - Синхронизация Рекомендация из History
- `sales_updateMetricsFromSteamWebAPI(itemNames)` - Получение данных из SteamWebAPI для расчета метрик
- `sales_calculateAllMetrics()` - Расчет всех метрик для позиций в Sales (Liquidity Score, Demand Ratio, Price Momentum, Sales Trend, Volatility Index, Hero Trend)
  - **Важно:** Метрики рассчитываются, но не записываются в таблицу (удалены из отображения)
  - Используются только для расчета Buyback Score и Risk Level
- `sales_updateBuybackScores()` - Расчет Buyback Score для всех позиций в Sales
- `sales_generateRecommendation(row)` - Генерация рекомендации на основе Buyback Score
- `sales_dailyReset()` - Очистка цен (вызывается при создании колонки периода)
  - Очищает текущие цены и расчеты
  - Синхронизация аналитики НЕ выполняется здесь (выполняется в `syncPricesFromHistoryToInvestAndSales()`)
- `sales_formatTable()` - Форматирование листа Sales

### PortfolioStats

- `portfolioStats_calculateMetrics_()` - Расчет текущих метрик портфеля (5 метрик: вложения, стоимость, прибыль, прибыль %, средняя прибыльность)
- `portfolioStats_saveHistory_()` - Сохранение метрик в историю (вставка строки сверху, форматирование)
- `portfolioStats_saveHistoryManual()` - Ручное сохранение истории (для тестирования, с уведомлением пользователя)
- `portfolioStats_formatTable()` - Форматирование листа PortfolioStats
- `portfolioStats_update()` - Обновление листа (вызывает форматирование)

### Utils

- `fetchWithRetry_(url, options, config)` - Универсальная функция для выполнения HTTP запросов с retry логикой (используется в SteamWebAPI и OpenDotaAPI)
  - Поддерживает экспоненциальную задержку при rate limit (429)
  - Использует константы HTTP_STATUS и LIMITS для магических чисел
- `history_createPeriodAndUpdate()` - Создание колонки периода и обновление цен (вызывается триггерами в 00:00 и 12:00)
  - При создании новой колонки: логирует начало обновления в AutoLog, обновляет текущие цены (окраска устаревших в желтый)
- `unified_priceUpdate()` - Продолжение сбора цен каждые 10 минут до завершения периода
- `updateAllMetricsForInvestSales()` - Универсальное обновление всех метрик для Invest и Sales
  - Вызывает расчет метрик (Liquidity, Demand, Momentum, Sales Trend, Volatility, Hero Trend) для всех позиций
  - **Важно:** Метрики рассчитываются, но не отображаются в таблицах (используются только для скоров)
  - Используется автоматически после синхронизации цен и вручную через меню
- `syncPricesFromHistoryToInvestAndSales()` - Синхронизация цен из History в Invest/Sales
  - Читает данные History один раз через `readHistoryDataForSync_()` для оптимизации
  - Вызывает `syncInvestPricesFromHistory_()` и `syncSalesPricesFromHistory_()` с общими данными
  - Автоматически вызывает batch-расчеты прибыли/просадки
  - Автоматически обновляет аналитику: `invest_syncMinMaxFromHistory()`, `invest_syncTrendDaysFromHistory()`, `invest_syncExtendedAnalyticsFromHistory()`, и аналогично для Sales
  - Автоматически вызывает `updateAllMetricsForInvestSales()` для расчета метрик после синхронизации цен
- `readHistoryDataForSync_(historySheet)` - Универсальная функция для чтения данных History (оптимизация)
- `getCurrentPriceFromHistoryData_(historyData)` - Универсальная функция для получения текущей цены из данных History
- `isPriceOutdated_(historyData)` - Универсальная функция для определения устаревшей цены
- `formatTableBase_(sheet, headers, columns, getSheetFn, sheetName)` - Универсальная функция для базового форматирования таблиц (FormattingService.gs)
- `formatNewRowUniversal_(sheet, row, config, numberFormatConfig, addImageAndLink)` - Универсальная функция для форматирования новых строк (FormattingService.gs)
- `getHistoryPriceForPeriod_(historySheet, itemName, period)` - Получение цены из History для текущего периода
- `acquireLock_(lockKey, timeoutSeconds)` - Блокировка для предотвращения параллельного выполнения (таймаут 5 минут)
- `releaseLock_(lockKey)` - Освобождение блокировки
- `getCurrentPricePeriod()` - Определение текущего периода ("ночь" или "день")
- `logAutoAction_(sheetName, action, status)` - Логирование автоматических действий в AutoLog
- `logOperation_(type, itemName, quantity, pricePerUnit, total, source)` - Логирование операций покупки/продажи в Log
- `insertLogRowUniversal_(sheet, values, formats)` - Универсальная функция для вставки строк в листы логов
- `createLogSheet_(sheetName, headers, columnWidths)` - Универсальная функция для создания листов логов (в SheetService.gs)

### Analytics

- `analytics_calculateLiquidityScore(itemData)` - Расчет Liquidity Score (оценка ликвидности) на основе sold24h, sold7d, offervolume, hourstosold
- `analytics_calculateDemandRatio(itemData)` - Расчет Demand Ratio (соотношение спроса и предложения) на основе buyordervolume и offervolume
- `analytics_calculatePriceMomentum(itemData, historyData)` - Расчет Price Momentum (импульс цены) на основе текущей цены, pricelatestsell7d/30d, priceavg7d
- `analytics_calculateSalesTrend(itemData)` - Расчет Sales Trend (тренд продаж) на основе sold24h, sold7d, sold30d
- `analytics_calculateVolatilityIndex(itemData, historyData)` - Расчет Volatility Index (индекс волатильности) на основе истории цен (прямая нормализация - высокая волатильность = лучше)
- `analytics_calculateHeroTrendScore(heroId, rankCategory, heroStats)` - Расчет Hero Trend Score (оценка тренда героя) на основе proContestRateChange7d, pickRateChange7d (Immortal), pickRatePercent (Immortal), winRate. Возвращает 0-1, затем умножается на 100 для отображения (0-100)
- `analytics_calculateMetaSignal(heroId, rankCategory, heroStats)` - Расчет Мета сигнала (краткосрочный индикатор патч-имб) на основе pickRateChange24h (Immortal), proContestRateChange7d, pickRateChange7d (Immortal). Возвращает 0-100
- `analytics_formatMetaSignal(score)` - Форматирование Мета сигнала для отображения (🔥 >=75, 🟡 >=60, ⚪ >=40, 🔴 <40)
- `analytics_calculateInvestmentScore(itemData, heroStats, historyData, itemCategory, heroId, rankCategory)` - Расчет Investment Score (итоговая оценка для покупки, 0-100 шкала) - взвешенная сумма всех компонентов
- `analytics_calculateBuybackScore(itemData, heroStats, historyData, sellPrice, currentPrice, heroId, rankCategory)` - Расчет Buyback Score (итоговая оценка для откупа, 0-100 шкала) - процент просадки + метрики
- `analytics_calculateRiskLevel(investmentScore, volatilityIndex, demandRatio)` - Расчет Risk Level (уровень риска: Низкий/Средний/Высокий)
- `analytics_normalizeMetric(value, min, max, inverse)` - Нормализация метрики в диапазон 0-1
- `analytics_normalizeToRange(value, min, max, targetMin, targetMax)` - Нормализация значения в целевой диапазон
- `analytics_formatScore(score)` - Форматирование скора для отображения (🟢 85, поддерживает оба формата: 0-1 и 0-100)

### SteamWebAPI

- `steamWebAPI_fetchItems(itemNames, game)` - Получение данных о предметах пакетами (до 50)
- `steamWebAPI_fetchSingleItem(itemName, game)` - Получение данных об одном предмете
- `steamWebAPI_getItemData(itemName, game)` - Унифицированный интерфейс для получения данных о предмете
  - **Автоматический fallback:** Если предмет не найден через основной endpoint `/api/item`, автоматически пробует `/api/item_by_nameid` с поиском по Market Hash Name
  - API автоматически определяет NameID из базы данных по названию
  - Используется в `fetchLowestPrice_()` для получения цен
- `steamWebAPI_getHeroNameFromTags(itemData)` - Извлечение имени героя из тегов предмета (tag5)
- `steamWebAPI_parseItemData(itemData)` - Парсинг данных предмета в удобный формат
- `steamWebAPI_fetchItemsBatch(itemNames, game)` - Получение данных для нескольких предметов пакетами
- `steamWebAPI_fetchItemByNameId(nameId)` - Получение данных по NameID
- `steamWebAPI_fetchItemByNameIdViaName(itemName, game)` - Получение данных по названию через item_by_nameid (fallback)
  - Использует параметр `name` для поиска по Market Hash Name
  - API автоматически определяет NameID из базы данных
  - Используется как fallback в `HeroMapping` и утилитах расчета Min/Max
- `steamWebAPI_testConnection()` - Тест подключения к API

### Утилиты для расчета Min/Max цен (в SteamWebAPI.gs)

- `priceHistory_calculateMinMaxForAllItems(onlyMissing)` - Расчет Min/Max для всех предметов из History через SteamWebAPI.ru
  - Использует SteamWebAPI.ru вместо pricehistory эндпоинта (быстрее, надежнее, до 50 предметов за раз)
  - Если `onlyMissing = true`, обновляет только предметы без Min/Max
- `priceHistory_calculateMinMaxForMissingItems()` - Расчет Min/Max только для предметов, у которых Min/Max отсутствуют
- `priceHistory_updateMinMaxInHistory(itemName, minPrice, maxPrice)` - Обновление Min/Max для одного предмета в History
- `priceHistory_checkMinMaxExists(itemName)` - Проверка наличия Min/Max у предмета в History
- `priceHistory_fetchHistoryForItem(itemName, appid)` - Получение истории цен через pricehistory эндпоинт Steam (fallback)
- `priceHistory_parseHistoryData(pricesData)` - Парсинг данных истории цен из формата pricehistory
- `priceHistory_calculateMinMax(parsedHistory)` - Расчет Min/Max из распарсенной истории
- `priceHistory_calculateMinMaxForSingleItem(itemName)` - Расчет Min/Max для одного предмета (вспомогательная функция)

### OpenDotaAPI

- `openDota_fetchHeroStats(rankTier)` - Получение статистики героев (пикрейт, винрейт, банрейт, про-статистика)
  - Возвращает про-статистику: `proPick`, `proBan`, `proContestRate` для каждой категории ранга
- `openDota_fetchAllHeroStats()` - Получение статистики для обеих категорий (High Rank + All Ranks)
  - Включает про-статистику (`proPick`, `proBan`, `proContestRate`) для каждой категории
- `openDota_testConnection()` - Тест подключения к API

### HeroMapping

- `heroMapping_syncWithHistory()` - Синхронизация предметов из History с HeroMapping (добавляет только новые предметы)
- `heroMapping_autoDetectFromSteamWebAPI()` - Автоматическое определение героев для предметов через SteamWebAPI.ru
- `heroMapping_fillMissingHeroIds()` - Автоматическое заполнение пустых Hero ID в HeroMapping из HeroStats по Hero Name
  - Создает Map Hero Name → Hero ID из HeroStats
  - Заполняет Hero ID для всех предметов с пустым Hero ID, но заполненным Hero Name
  - Возвращает `{filled: number, notFound: number}`
  - Автоматически вызывается в `setupHeroStats()` и `autoSyncHeroMapping()`
- `heroMapping_getHeroForItem(itemName)` - Получение информации о герое для предмета
- `heroMapping_getAllMappings()` - Получение всех маппингов (используется для синхронизации статистики)

### Telegram

- `telegram_setConfig(botToken, chatId)` - Сохранение конфигурации бота (в PropertiesService)
- `telegram_getConfig()` - Получение конфигурации бота
- `telegram_sendMessage(message, parseMode, disablePreview)` - Отправка сообщения
  - `disablePreview` (по умолчанию `true`) - отключает превью ссылок для компактности
  - `parseMode` обычно 'HTML' для форматирования
- `telegram_checkDailyPriceTargets()` - Ежедневная проверка цен и отправка отчетов (триггер 13:00)
  - Отправляет ежедневный отчет через `telegram_sendDailyReport()` (топ-5 покупок и топ-5 откупов)
  - Отправляет одно группированное сообщение со всеми позициями, достигшими цели
  - Убраны отдельные сообщения о просадке (просадки не отправляются в рамках триггера 13:00)
  - Исправлен баг: теперь корректно читает колонки POTENTIAL, MAX_PRICE, RECOMMENDATION из Invest
- `telegram_sendDailyReport()` - Отправка ежедневного отчета о портфеле
  - Отправляет 2 сообщения: топ-5 возможностей из History (Investment Score >= 75, включая в портфеле для докупки), топ-5 откупов из Sales (Buyback Score >= 75)
  - Топ-5 покупок включает потенциал (P85) и тренд героя
  - Топ-5 откупов включает Buyback Score рядом с названием, потенциал (P85) и Max цену из History
  - Общие метрики портфеля убраны (оставлены только для PortfolioStats)
- `telegram_checkHistoryInvestmentOpportunities_()` - Проверка Investment Score из History для предметов НЕ в портфеле
  - Отправляет критические уведомления для предметов с Investment Score >= 75
  - Вызывается автоматически после обновления Investment Scores
  - Использует cooldown для предотвращения спама
  - Примечание: В ежедневном отчёте (топ-5 покупок) показываются все предметы с Investment Score >= 75, включая те, что уже в портфеле (для докупки)
- `telegram_checkSalesBuybackOpportunities_()` - Проверка Buyback Score из Sales
  - Отправляет критические уведомления для предметов с Buyback Score >= 75
  - Вызывается автоматически после обновления Buyback Scores
  - Использует cooldown для предотвращения спама
  - Использует Max цену из History (синхронизированную в Sales) для отображения "макс" в уведомлениях
- `telegram_checkMetaSignalOpportunities_()` - Проверка Мета сигнала (горячие уведомления о патч-имбах)
  - Отправляет уведомления для предметов с Мета сигналом >= 70 (краткосрочный индикатор)
  - Максимум 5 уведомлений за раз (топ по Мета сигналу)
  - Использует cooldown 6 часов (короткий период для быстрых сигналов)
- `telegram_checkHeroTrendChanges_()` - Проверка изменений Hero Trend Score за 24 часа
  - Отправляет важные уведомления для предметов с изменением > 15% за 24ч
  - Группирует топ-5 в одном сообщении
  - Вызывается автоматически после `history_syncHeroStats()`
  - Сохраняет предыдущие значения в PropertiesService для сравнения
- `telegram_checkPriceChanges_()` - Проверка резких изменений цены (>20% за 24ч)
  - Отправляет критические уведомления немедленно
  - Сравнивает текущую цену с ценой 24 часа назад из History
  - Вызывается автоматически после обновления цен в `unified_priceUpdate()`
- `telegram_checkNotificationSent_(type, itemName, eventId, cooldownMs)` - Проверка cooldown для уведомления
  - Использует PropertiesService для быстрой проверки
  - Возвращает `true` если уведомление уже было отправлено и cooldown еще не истек
- `telegram_saveNotification_(type, itemName, eventId, data, priority)` - Сохранение информации об отправленном уведомлении
  - Сохраняет в PropertiesService (для cooldown) и в лист TelegramNotifications (для истории)
- `telegram_cleanupOldNotifications_()` - Очистка старых уведомлений из PropertiesService (старше 7 дней)
  - Вызывается автоматически каждое воскресенье в 03:00 (вместе с архивацией HeroStats)
  - История в листе TelegramNotifications хранится бессрочно
- `telegram_parseScore_(formattedScore)` - Парсинг форматированного скора (🟢 0.93) в число
- `getOrCreateTelegramNotificationsSheet_()` - Получение или создание листа TelegramNotifications
  - Структура: Дата/Время, Тип, Предмет, Событие, Данные (JSON), Приоритет
- `telegram_testConnection()` - Тест подключения к боту
- `telegram_testDailyNotifications()` - Ручной тест ежедневных уведомлений

### Menu

- `onOpen()` - Создание меню при открытии таблицы
- `setupAllTriggers()` - Создание всех триггеров (включая автоматическое обновление статистики героев, синхронизацию HeroMapping, архивацию данных и Telegram)
- `removeAllTriggers()` - Удаление всех триггеров
- `initializeAllTables()` - Инициализация всех таблиц (форматирование)
- `performInitialSetup()` - Первоначальная настройка новой таблицы (Min/Max, HeroMapping, статистика героев) - с диалогами для каждого шага
- `performFullSetup()` - Полная настройка - последовательно вызывает все шаги (setupMinMax → setupHeroMapping → setupHeroStats → setupAnalytics)
- `setupMinMax()` - Шаг 0: Расчет Min/Max из SteamWebAPI для отсутствующих предметов (опционально, выполняется автоматически в performFullSetup)
- `setupHeroMapping()` - Шаг 1: Синхронизация HeroMapping с History и автоопределение героев
- `setupHeroStats()` - Шаг 2: Обновление статистики героев через OpenDota API, автоматическое заполнение пустых Hero ID в HeroMapping из HeroStats, синхронизация в History
- `setupAnalytics()` - Шаг 3: Обновление аналитики History (включая Min/Max из существующих цен) и Investment Scores
- `checkSystemReadiness()` - Проверка готовности системы (наличие данных, заполненность Min/Max, наличие HeroMapping и HeroStats)
- `checkAutomationStatus()` - Проверка состояния автоматизации (активные триггеры, последние выполнения)
- `syncAnalyticsForInvestSales_()` - Ручная синхронизация аналитики Invest/Sales (Min/Max, Тренд, Фаза/Потенциал/Рекомендация)
  - Автоматически вызывает `updateAllMetricsForInvestSales()` для расчета метрик после синхронизации
- `updateAllMetricsAndScores_()` - Комплексное обновление всех метрик и скоров для Invest/Sales/History
  - Обновляет метрики (Liquidity, Demand, Momentum, Sales Trend, Volatility) для Invest/Sales
  - Обновляет Investment Scores для History и Invest
  - Обновляет Buyback Scores для Sales
  - Синхронизирует статистику героев
- `autoUpdateHeroStats()` - Автоматическое обновление статистики героев (для триггера)
  - Выполняет полный цикл: обновление статистики → синхронизация в History → обновление Investment Scores → обновление Buyback Scores
  - Использует блокировку для предотвращения параллельного выполнения
  - Логирует все действия в AutoLog
  - Отправляет уведомления в Telegram при критических ошибках
- `autoArchiveHeroStats()` - Автоматическая архивация старых данных HeroStats (для триггера)
  - Удаляет данные старше 90 дней
  - Использует блокировку для предотвращения параллельного выполнения
  - Логирует действия в AutoLog
- `autoSyncHeroMapping()` - Автоматическая синхронизация HeroMapping (для триггера)
  - Синхронизирует предметы из History
  - Автоматически определяет героев для новых предметов
  - Автоматически заполняет пустые Hero ID из HeroStats
  - Использует блокировку для предотвращения параллельного выполнения
  - Логирует действия в AutoLog

### EventHandler

- `onEdit(e)` - Обработка изменений в листах (чекбокс "Купить" в History и "Продать" в Invest)
- Обработка чекбокса "Купить" в History: создание/обновление позиции в Invest
- Обработка чекбокса "Продать" в Invest: обновление количества или удаление строки, создание/обновление записи в Sales

---

## КОНСТАНТЫ И КОНФИГУРАЦИЯ

### Основные константы (Constants.gs)

```javascript
STEAM_APP_ID = 570  // Dota 2
STEAM_FEE = 0.85    // Комиссия Steam 15%

DATA_START_ROW = 2
HEADER_ROW = 1
ROW_HEIGHT = 85
HEADER_HEIGHT = 35

NUMBER_FORMATS = {
  CURRENCY: '#,##0.00 ₽',
  PERCENT: '0.00%',
  INTEGER: '0'
}

COLORS = {
  PROFIT: '#c8e6c9',      // Светло-зеленый
  LOSS: '#ffcdd2',        // Светло-красный
  STABLE: '#fff9c4',      // Желтый
  BACKGROUND: '#f3f3f3'   // Серый для заголовков
}

HTTP_STATUS = {
  OK: 200,
  RATE_LIMIT: 429
}

UPDATE_INTERVALS = {
  PRICES_MINUTES: 10,     // Каждые 10 минут продолжение сбора
  MORNING_HOUR: 0,        // 00:00 (начало периода "ночь")
  EVENING_HOUR: 12        // 12:00 (начало периода "день")
}

PRICE_COLLECTION_PERIODS = {
  MORNING: 'morning',     // Период "ночь" (00:10-12:00)
  EVENING: 'evening'      // Период "день" (12:00-00:10)
}

LIMITS = {
  TIME_BUDGET_MS: 330000,  // 5.5 минут на операцию
  MAX_ATTEMPTS: 2,         // Попыток получения цены
  BASE_DELAY_MS: 200,      // Базовая задержка
  BETWEEN_ITEMS_MS: 150,   // Задержка между предметами
  LOCK_TIMEOUT_SEC: 300,   // Таймаут блокировки (5 минут)
  ERROR_MESSAGE_MAX_LENGTH: 200,  // Максимальная длина сообщения об ошибке
  // Дополнительные задержки
  API_RETRY_DELAY_MS: 500,  // Задержка между повторными попытками API
  API_BETWEEN_ITEMS_MS: 1000,  // Задержка между предметами при batch запросах
  API_BETWEEN_BATCHES_MS: 2000,  // Задержка между пакетами запросов
  TELEGRAM_MESSAGE_DELAY_MS: 500,  // Задержка между сообщениями Telegram
  TELEGRAM_REPORT_DELAY_MS: 1000,  // Задержка между отчетами Telegram
  EVENT_HANDLER_DELAY_MS: 100,  // Задержка в обработчике событий
  HISTORY_UPDATE_DELAY_MS: 100,  // Задержка при обновлении History
  METRICS_UPDATE_DELAY_MS: 500,  // Задержка при обновлении метрик
  // Таймауты для операций
  SAFETY_BUFFER_MS: 1000,  // Буфер безопасности перед превышением таймаута
  MIN_INTERVAL_MS: 100,  // Минимальный интервал между запросами
  MAX_ERROR_DELAY_MS: 250,  // Максимальная задержка при ошибках
  // Размеры колонок для логов
  LOG_COLUMN_WIDTHS: {
    AUTO_LOG: [150, 100, 200, 100],  // Дата/время, Лист, Действие, Статус
    LOG: [150, 90, 120, 250, 90, 120, 120, 120]  // Дата/время, Тип, Изображение, Предмет, Кол-во, Цена за шт, Сумма, Источник
  }
}

API_CONFIG = {
  STEAM_WEB_API: {
    BASE_URL: 'https://steamwebapi.ru',
    TIMEOUT_MS: 30000,
    MAX_RETRIES: 3,
    RETRY_DELAY_MS: 500,
    MAX_ITEMS_PER_REQUEST: 50  // До 50 предметов за один запрос
  },
  OPENDOTA: {
    BASE_URL: 'https://api.opendota.com/api',
    TIMEOUT_MS: 30000,
    MAX_RETRIES: 3,
    RETRY_DELAY_MS: 500
  }
}

HERO_STATS_UPDATE_SCHEDULE = {
  HOURS: [2, 10, 18]  // 02:00, 10:00, 18:00 UTC
}

HERO_STATS_HISTORY_DAYS = 90  // Хранить 90 дней истории

// Веса для расчета скоров (Analytics)
ANALYTICS_WEIGHTS = {
  INVESTMENT_SCORE: {
    HERO_TREND: 0.35,
    VOLATILITY: 0.20,
    DEMAND_RATIO: 0.20,
    PRICE_MOMENTUM: 0.15,
    LIQUIDITY: 0.05,
    SALES_TREND: 0.05
  },
  BUYBACK_SCORE: {
    PRICE_DROP: 0.35,
    VOLATILITY: 0.25,
    HERO_TREND: 0.20,
    DEMAND_RATIO: 0.10,
    PRICE_MOMENTUM: 0.05,
    LIQUIDITY: 0.05
  },
  HERO_TREND_SCORE: {
    PRO_CONTEST_RATE_CHANGE_7D: 0.40,  // Про-мета за неделю (водопадная мета)
    PICK_RATE_CHANGE_IMMORTAL_7D: 0.25,  // Immortal за неделю
    PICK_RATE_IMMORTAL: 0.20,  // Текущая популярность Immortal
    WIN_RATE: 0.05  // Вспомогательный
    // Убрано: CONTEST_RATE_CHANGE (фейк, дублировал pickRateChange)
    // Убрано: BAN_RATE (всегда 0 для обычных матчей)
    // PICK_RATE_CHANGE_IMMORTAL_24H переходит в Мета сигнал
  },
  META_SIGNAL: {
    PICK_RATE_CHANGE_IMMORTAL_24H: 0.50,  // Главный индикатор патч-имб
    PRO_CONTEST_RATE_CHANGE_7D: 0.30,    // Про-мета (водопадная)
    PICK_RATE_CHANGE_IMMORTAL_7D: 0.20   // Недельный тренд Immortal
  }
}

// Пороги для критических сигналов
ANALYTICS_THRESHOLDS = {
  INVESTMENT_SCORE_CRITICAL: 75,  // 0-100 шкала
  BUYBACK_SCORE_CRITICAL: 75,     // 0-100 шкала
  HERO_CHANGE_24H: 0.15,      // 15% за 24 часа
  HERO_CHANGE_7D: 0.10,        // 10% за 7 дней
  PRICE_CHANGE_24H: 0.20       // 20% за 24 часа
}

// Константы для системы Telegram уведомлений
TELEGRAM_NOTIFICATION_TYPES = {
  INVESTMENT_SCORE: 'investment_score',
  BUYBACK_SCORE: 'buyback_score',
  HERO_TREND: 'hero_trend',
  PRICE_CHANGE: 'price_change',
  GOAL_REACHED: 'goal_reached',
  DROP: 'drop'
}

TELEGRAM_PRIORITY = {
  CRITICAL: 'critical',
  IMPORTANT: 'important',
  INFO: 'info'
}

TELEGRAM_COOLDOWN_MS = {
  GOAL_REACHED: 24 * 60 * 60 * 1000,        // 24 часа
  DROP: 24 * 60 * 60 * 1000,                 // 24 часа
  INVESTMENT_SCORE: 6 * 60 * 60 * 1000,      // 6 часов
  BUYBACK_SCORE: 6 * 60 * 60 * 1000,         // 6 часов
  HERO_TREND: 12 * 60 * 60 * 1000,           // 12 часов
  PRICE_CHANGE: 24 * 60 * 60 * 1000          // 24 часа
}
```

### Конфигурация анализа трендов

```javascript
TREND_ANALYSIS_CONFIG = {
  SIMPLE_COMPARISON: {
    BASE_THRESHOLD: 0.08,      // 8%
    VOLATILITY_MULTIPLIER: 1.5,
    SIDEWAYS_FACTOR: 0.4
  },
  MOVING_AVERAGES: {
    SHORT_WINDOW: 3,
    LONG_WINDOW: 7,
    BASE_THRESHOLD: 0.02        // 2%
  },
  LINEAR_REGRESSION: {
    WINDOW: 20,
    GROWTH_THRESHOLD: 0.03,     // 3%
    FALL_THRESHOLD: -0.03        // -3%
  },
  MOMENTUM: {
    WINDOW: 5,
    BASE_THRESHOLD: 0.05         // 5%
  },
  CYCLE_PHASE: {
    BOTTOM_THRESHOLD: 0.25,      // 25%
    TOP_THRESHOLD: 0.75          // 75%
  },
  GROWTH_POTENTIAL: {
    PERCENTILE_TARGET: 0.85      // 85-й перцентиль
  }
}
```

### Структура колонок History

```javascript
HISTORY_COLUMNS = {
  IMAGE: 'A',
  NAME: 'B',
  STATUS: 'C',
  LINK: 'D',
  BUY_CHECKBOX: 'E',
  CURRENT_PRICE: 'F',
  MIN_PRICE: 'G',
  MAX_PRICE: 'H',
  INVESTMENT_SCORE: 'I',
  RECOMMENDATION: 'J',
  PHASE: 'K',
  POTENTIAL: 'L',
  TREND: 'M',  // Объединенный формат: "🟥 Падает 35 дн."
  HERO_TREND: 'N',
  CONTEST_RATE_CHANGE_7D: 'O',
  CONTEST_RATE_CURRENT: 'P',
  PICK_RATE_CURRENT: 'Q',
  WIN_RATE_CURRENT: 'R',
  HERO_NAME: 'S',
  FIRST_DATE_COL: 20  // Колонка T (было 21, убрали DAYS_CHANGE)
}
```

---

## РАСЧЕТЫ И ФОРМУЛЫ

### Invest - Расчет прибыли

```javascript
totalInvestment = quantity * buyPrice
currentValue = quantity * currentPrice
currentValueAfterFee = currentValue * STEAM_FEE  // × 0.85
profit = currentValueAfterFee - totalInvestment
profitAfterFee = (currentValueAfterFee / totalInvestment) - 1
```

### Invest - Усреднение цены покупки

При добавлении позиции к существующей:
```javascript
newQty = currentQty + qtyToAdd
newAvg = (currentQty * currentBuy + qtyToAdd * buyPricePerUnit) / newQty
```

### Sales - Расчет просадки цены

```javascript
priceDrop = (sellPrice - currentPrice) / sellPrice
```
- Положительное значение = цена выросла (хорошо)
- Отрицательное значение = цена упала (плохо)

### PortfolioStats - Метрики портфеля

```javascript
totalInvestment = сумма всех totalInvestment из Invest
totalCurrentValue = сумма всех currentValueAfterFee из Invest
totalProfit = totalCurrentValue - totalInvestment
totalProfitPercent = (totalCurrentValue / totalInvestment) - 1
avgProfitability = средняя прибыльность всех позиций (profitAfterFee)
```

---

## ЧЕКЛИСТ ПРОВЕРКИ ПРИ ИЗМЕНЕНИЯХ

При изменении кода проверять:

- [ ] Группировка цен по дате используется везде, где нужно (тренды, фазы, потенциал)
- [ ] Потенциал хранится как число, не строка
- [ ] Порядок форматирования: сначала условное (`history_applyAllConditionalFormatting_`), потом прямое (`history_highlightMinMax_`)
- [ ] Batch-операции используются для оптимизации (чтение/запись группами)
- [ ] Блокировки освобождаются в `finally` блоке
- [ ] Обработка граничных случаев (null, 0, отрицательные, `Number.isFinite()`)
- [ ] Синхронизация данных между листами работает (History → Invest/Sales)
- [ ] Новые записи вставляются сверху (используется `insertRowAfter(HEADER_ROW)`)
- [ ] API запросы используют retry логику и обработку ошибок (429, таймауты)
- [ ] Fallback механизмы работают (например, item_by_nameid для предметов, не найденных через основной endpoint)
  - `steamWebAPI_getItemData()` автоматически использует fallback на `/api/item_by_nameid` с поиском по Market Hash Name
  - `HeroMapping` и утилиты расчета Min/Max в `SteamWebAPI.gs` также используют fallback для не найденных предметов
- [ ] Batch операции для API запросов (SteamWebAPI.ru поддерживает до 50 предметов за раз)

---

## ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: Ошибки при создании колонок в 00:00

**Симптомы**: Ошибки в логах в 00:07, когда триггер пытается обновить цены, но колонка еще не создана

**Решение**: 
- Разрешено создание колонки "ночь" с 00:00 (вместо строго 00:10)
- `history_updatePricesForPeriod` возвращает `false` вместо выброса ошибки
- `unified_priceUpdate` корректно обрабатывает `false`

**Файлы**: `History.gs`, `Utils.gs`

---

## МЕНЮ И ТРИГГЕРЫ

### Главное меню SteamTable

- Включить/Выключить автообновление
- Инициализировать все таблицы
- Обновить цены History (ручное)

### Подменю Invest
- Форматирование
- Изображение и ссылки
- Поиск дублей

### Подменю Sales
- Форматирование
- Изображение и ссылки
- Поиск дублей

### Подменю History
- Форматирование
- Обновить аналитику
- Изображения и ссылки
- Дубли названий
- Создать столбец текущего периода

### Подменю PortfolioStats
- Форматирование
- Сохранить строку истории (тест)

### Подменю HeroMapping
- Форматирование
- Автоопределение героев
- Синхронизировать с History
- Заполнить пустые Hero ID

### Подменю Telegram
- Настроить Telegram
- Тест Telegram
- Тест ежедневных уведомлений

### Подменю Синхронизация
- Синхронизировать цены из History
- Обновить аналитику Invest/Sales (включает автоматический расчет метрик)
- Обновить метрики Invest/Sales (ручное обновление метрик)
- Обновить все метрики и скоры
