# ДОКУМЕНТАЦИЯ ПРОЕКТА SteamTable

> **Назначение**: Этот документ предназначен для AI агентов и содержит актуальную техническую информацию о структуре проекта, функциях, логике работы и важных деталях реализации.

---

## ПРАВИЛА РАБОТЫ С ДОКУМЕНТАЦИЕЙ

### Для AI агентов:

1. **Перед выполнением задачи**: Обязательно прочитай этот документ для синхронизации контекста
2. **После изменений в коде**: Актуализируй документацию - обнови описание функций, добавь важные технические детали, измени структуру если нужно
3. **Не хранить**: Историю выполненных задач, даты изменений, информацию о том "что было реализовано"
4. **Хранить**: Текущую структуру проекта, актуальное описание функций, технические детали реализации, известные проблемы и их решения

---

## СТРУКТУРА ПРОЕКТА

### Файлы кода:

1. **Constants.gs** - Константы и конфигурация (STEAM_FEE, COLORS, NUMBER_FORMATS, TREND_ANALYSIS_CONFIG)
2. **SheetService.gs** - Утилиты для работы с листами (`getOrCreateSheet_`, `formatHeaderRange_`)
3. **History.gs** - Основная логика листа History (цены, тренды, аналитика)
4. **Invest.gs** - Логика листа Invest (портфель инвестиций, расчет прибыли)
5. **Sales.gs** - Логика листа Sales (проданные предметы, расчет просадки)
6. **Utils.gs** - Унифицированная система обновления цен, синхронизация, блокировки
7. **Menu.gs** - Меню и триггеры (onOpen, setupAllTriggers)
8. **EventHandler.gs** - Обработка событий листов (чекбоксы "Купить?" и "Продать?")
9. **FormattingService.gs** - Утилиты форматирования
10. **Calculations.gs** - Batch-расчеты прибыли/убытка для Invest/Sales
11. **PortfolioStats.gs** - Трекинг истории портфеля (сохранение метрик)
12. **Telegram.gs** - Интеграция с Telegram Bot API (уведомления, отчеты)

### Листы таблицы:

- **History** - Основной лист с историей цен по датам
- **Invest** - Портфель активных инвестиций
- **Sales** - Проданные предметы (отслеживание просадки цены)
- **PortfolioStats** - История метрик портфеля (динамика во времени)
- **AutoLog** - Лог автоматических действий скрипта
- **Log** - Лог операций покупки/продажи

---

## КРИТИЧЕСКИ ВАЖНЫЕ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Группировка цен по дате

**ОБЯЗАТЕЛЬНО**: Все функции, работающие с историей цен, должны группировать цены по дате (игнорируя период "ночь"/"день").

**Почему**: Период "ночь" (00:10-12:00) и "день" (12:00-00:10) относятся к одной календарной дате. Для анализа трендов, фазы цикла и потенциала нужна одна цена на день.

**Как группировать**:
1. Собрать все цены с их датами из заголовков колонок
2. Парсить дату из заголовка (формат: "dd.MM.yy ночь" или "dd.MM.yy день")
3. Группировать по ключу `dd.MM.yy`, беря **последнюю цену** за день
4. Отсортировать даты в хронологическом порядке

**Где используется**:
- `history_analyzeTrend()` - анализ тренда
- `history_updateTrends()` - расширенная аналитика (фаза, потенциал)
- `history_calculateDaysChange_()` - расчет дней смены тренда

**Важно для расчета дней**:
- Уникальный список дат создается из Map (без дубликатов) - гарантирует одну запись на день
- При создании объектов Date используется полдень (12:00:00) для избежания проблем с часовыми поясами
- Для расчета разницы в днях используется `Math.floor` (точный подсчет календарных дней, не округление вверх)
- Разница вычисляется между реальными датами, а не количеством записей

### Формат данных для потенциала

**Потенциал хранится как число**, не строка:
- Число: `0.14` (для +14%)
- Форматирование: `'+0%;-0%;"—"'` для отображения "+14%" или "-5%"
- Это позволяет сортировку и правильные математические операции

**Файлы**: `History.gs`, `Invest.gs`, `Sales.gs`, `Utils.gs`

### Порядок применения форматирования

**ОБЯЗАТЕЛЬНЫЙ порядок**:
1. Сначала условное форматирование (`history_applyAllConditionalFormatting_`)
2. Потом прямое форматирование - выделение min/max (`history_highlightMinMax_`)

**Почему**: Прямое форматирование перезаписывает условное. Если порядок нарушен, min/max не будут выделены.

**Файл**: `History.gs`, функция `history_updateAllAnalytics_()`

### Обработка ошибок при создании колонок периодов

**Проблема**: Триггер настроен на 00:00, но проверка времени требовала 00:10, что вызывало ошибки.

**Решение**:
- Разрешено создание колонки "ночь" с 00:00 до 12:00 (вместо строго 00:10)
- `history_updatePricesForPeriod` возвращает `false` вместо выброса ошибки, если колонка не создана
- `unified_priceUpdate` корректно обрабатывает `false` - просто выходит без ошибки

**Файлы**: `History.gs`, `Utils.gs`

### Вставка новых записей

**Все новые записи вставляются сверху** (после заголовка):
- Используется `sheet.insertRowAfter(HEADER_ROW)` 
- Применяется для: логов (AutoLog, Log), истории PortfolioStats

**Файлы**: `Utils.gs`, `PortfolioStats.gs`

---

## ЛОГИКА РАБОТЫ СИСТЕМЫ

### Поток данных

```
Steam Market API
    ↓
History (сохранение цен по датам)
    ↓
Синхронизация цен в Invest/Sales (syncPricesFromHistoryToInvestAndSales)
    ↓
Расчет аналитики в History (тренды, фазы, потенциал)
    ↓
Синхронизация аналитики в Invest/Sales
    ↓
Сохранение истории портфеля (PortfolioStats, только после периода "день")
```

### Автоматическое обновление (триггеры)

**Архитектура**:
1. **00:00** - `history_createPeriodAndUpdate_morning()` создает колонку периода "ночь" и начинает сбор цен
2. **12:00** - `history_createPeriodAndUpdate_evening()` создает колонку периода "день" и начинает сбор цен
3. **Каждые 10 минут** - `unified_priceUpdate()` продолжает сбор цен до завершения периода
4. **13:00** - `telegram_checkDailyPriceTargets()` отправляет ежедневные уведомления

**Периоды сбора цен**:
- **"ночь"**: 00:10 - 12:00 (техническое название для периода 00:10-12:00)
- **"день"**: 12:00 - 00:10 следующего дня (техническое название для периода 12:00-00:10)

**Завершение периода**:
Когда сбор цен завершен (`historyCompleted === true`):
1. Обновляется аналитика в History (тренды, фазы, min/max)
2. Синхронизируется аналитика в Invest/Sales
3. Если период === "день" → сохраняется история в PortfolioStats

**Важно**: Аналитика Invest/Sales обновляется автоматически внутри `syncPricesFromHistoryToInvestAndSales()`, поэтому при завершении периода вызывается только сохранение истории портфеля.

### Синхронизация данных

**Источник данных - History**:
- **Min/Max**: Из всех цен в колонках дат (поиск минимума/максимума)
- **Тренд/Дни смены**: Из колонок I (тренд) History
- **Фаза/Потенциал/Рекомендация**: Из колонок J, K, L History

**Функции синхронизации**:
- `invest_syncMinMaxFromHistory()` / `sales_syncMinMaxFromHistory()`
- `invest_syncTrendDaysFromHistory()` / `sales_syncTrendDaysFromHistory()`
- `invest_syncExtendedAnalyticsFromHistory()` / `sales_syncExtendedAnalyticsFromHistory()`

---

## СТРУКТУРА КОЛОНОК

### History (основной лист)

- **A**: Изображение
- **B**: Название предмета
- **C**: Статус (✓, ❌, ⚠️, 🆕, ❓)
- **D**: Ссылка на Steam Market
- **E**: Чекбокс "Купить?"
- **F**: Текущая цена
- **G**: Min (минимальная цена за историю)
- **H**: Max (максимальная цена за историю)
- **I**: Тренд - **объединенный формат** ("🟥 Падает 35 дн." или "🟩 Растет 12 дн.")
- **J**: Фаза цикла (Дно, Рост, Пик, Спад)
- **K**: Потенциал (P85) - **число** (0.14 для +14%), форматирование '+0%;-0%;"—"'
- **L**: Рекомендация (текстовая рекомендация на основе аналитики)
- **M+**: Колонки с датами (формат заголовка: "dd.MM.yy ночь" или "dd.MM.yy день")

### Invest (портфель инвестиций)

- **A**: Изображение
- **B**: Название
- **C**: Количество
- **D**: Цена покупки (усредненная при добавлении)
- **E**: Текущая цена (синхронизируется из History)
- **F**: Цель (целевая цена продажи)
- **G**: Общие вложения (количество × цена покупки)
- **H**: Текущая стоимость с комиссией (количество × текущая цена × 0.85)
- **I**: Профит в рублях (текущая стоимость с комиссией - вложения)
- **J**: Прибыль % с комиссией ((текущая стоимость с комиссией / вложения) - 1)
- **K**: Ссылка
- **L**: Min (из History)
- **M**: Max (из History)
- **N**: Тренд (из History, формат "🟥 Падает 35 дн.")
- **O**: Фаза (из History)
- **P**: Потенциал (P85) - **число** (из History)
- **Q**: Рекомендация (из History)
- **R**: Чекбокс "Продать?"

### Sales (проданные предметы)

- **A**: Изображение
- **B**: Название
- **C**: Цена продажи (максимальная, если продано несколько раз)
- **D**: Текущая цена (синхронизируется из History)
- **E**: Процент просадки ((цена продажи - текущая цена) / цена продажи)
- **F**: Ссылка
- **G**: Min (из History)
- **H**: Max (из History)
- **I**: Тренд (из History)
- **J**: Фаза (из History)
- **K**: Потенциал (P85) - **число** (из History)
- **L**: Рекомендация (из History)

### PortfolioStats (история портфеля)

- **A**: Дата/Время (формат: 'dd.MM.yyyy HH:mm')
- **B**: Вложения (сумма всех вложений в активные позиции)
- **C**: Текущая стоимость (сумма текущей стоимости всех позиций с учетом комиссии)
- **D**: Прибыль/убыток в рублях
- **E**: Прибыль/убыток % (общий процент от вложений)
- **F**: Средняя прибыльность (средняя прибыльность всех позиций)

**Особенности**:
- Новые записи вставляются сверху (после заголовка)
- Сохранение происходит автоматически 1 раз в сутки после завершения периода "день"
- Есть ручная функция для тестирования (`portfolioStats_saveHistoryManual()`)

---

## ВАЖНЫЕ ФУНКЦИИ И ИХ НАЗНАЧЕНИЕ

### History

- `history_analyzeTrend(row)` - Анализ тренда для одной строки (4 метода с голосованием, с группировкой по дате)
  - Создает уникальный список дат из Map (ночь и день одной даты = одна запись)
  - Использует полдень (12:00:00) при создании объектов Date для точности расчета
- `history_updateTrends()` - Обновление трендов для всех строк (оптимизированная версия с batch-чтением)
  - Создает уникальный список дат из Map (ночь и день одной даты = одна запись)
  - Использует полдень (12:00:00) при создании объектов Date для точности расчета
- `history_updateAllAnalytics_()` - Полное обновление аналитики: тренды, дни смены, фазы, потенциал, рекомендации, min/max
- `history_calculateDaysChange_(prices, dates, currentTrend)` - Расчет дней смены тренда
  - Использует разницу между реальными датами (Math.floor), а не количество записей
  - Требует, чтобы dates были уже сгруппированы по дате (одна запись на день)
  - Использует полдень (12:00:00) в датах для точности расчета
- `history_highlightMinMax_(sheet)` - Выделение ячеек с минимальными и максимальными ценами
- `history_applyAllConditionalFormatting_(sheet)` - Применение условного форматирования для трендов
- `history_ensurePeriodColumn(period)` - Создание колонки для периода (с проверкой времени)
- `history_updatePricesForPeriod(period)` - Обновление цен для текущего периода
- `history_formatTable()` - Форматирование листа History
- `history_updateImagesAndLinks()` - Обновление изображений и ссылок из Steam API

### Invest

- `invest_calculateBatch_(sheet, currentPrices)` - Batch-расчет прибыли для всех строк
- `invest_calculateSingle_(sheet, row, currentPrice)` - Расчет прибыли для одной строки
- `invest_syncMinMaxFromHistory()` - Синхронизация Min/Max из History
- `invest_syncTrendDaysFromHistory()` - Синхронизация Тренд/Дни из History
- `invest_syncExtendedAnalyticsFromHistory()` - Синхронизация Фаза/Потенциал/Рекомендация из History
- `invest_dailyReset()` - Очистка цен и пересчетов (вызывается при создании колонки периода)
- `invest_formatTable()` - Форматирование листа Invest
- `invest_formatGoalColumn_(startRow, endRow)` - Форматирование колонки "Цель" (красный/зеленый в зависимости от текущей цены)

### Sales

- `sales_calculateBatch_(sheet, currentPrices, sellPrices)` - Batch-расчет просадки цены
- `sales_calculateSingle_(sales, row, currentPrice)` - Расчет просадки для одной строки
- `sales_syncMinMaxFromHistory()` - Синхронизация Min/Max
- `sales_syncTrendDaysFromHistory()` - Синхронизация Тренд/Дни
- `sales_syncExtendedAnalyticsFromHistory()` - Синхронизация Фаза/Потенциал/Рекомендация
- `sales_dailyReset()` - Очистка цен (вызывается при создании колонки периода)
- `sales_formatTable()` - Форматирование листа Sales

### PortfolioStats

- `portfolioStats_calculateMetrics_()` - Расчет текущих метрик портфеля (5 метрик: вложения, стоимость, прибыль, прибыль %, средняя прибыльность)
- `portfolioStats_saveHistory_()` - Сохранение метрик в историю (вставка строки сверху, форматирование)
- `portfolioStats_saveHistoryManual()` - Ручное сохранение истории (для тестирования, с уведомлением пользователя)
- `portfolioStats_formatTable()` - Форматирование листа PortfolioStats
- `portfolioStats_update()` - Обновление листа (вызывает форматирование)

### Utils

- `history_createPeriodAndUpdate()` - Создание колонки периода и обновление цен (вызывается триггерами в 00:00 и 12:00)
- `unified_priceUpdate()` - Продолжение сбора цен каждые 10 минут до завершения периода
- `syncPricesFromHistoryToInvestAndSales()` - Синхронизация цен из History в Invest/Sales
  - Вызывает `syncInvestPricesFromHistory_()` и `syncSalesPricesFromHistory_()`
  - Автоматически вызывает batch-расчеты прибыли/просадки
  - Автоматически обновляет аналитику: `invest_syncMinMaxFromHistory()`, `invest_syncTrendDaysFromHistory()`, `invest_syncExtendedAnalyticsFromHistory()`, и аналогично для Sales
- `getHistoryPriceForPeriod_(historySheet, itemName, period)` - Получение цены из History для текущего периода
- `acquireLock_(lockKey, timeoutSeconds)` - Блокировка для предотвращения параллельного выполнения (таймаут 5 минут)
- `releaseLock_(lockKey)` - Освобождение блокировки
- `getCurrentPricePeriod()` - Определение текущего периода ("ночь" или "день")
- `logAutoAction_(action, description, status)` - Логирование автоматических действий в AutoLog
- `logOperation_(action, itemName, details)` - Логирование операций покупки/продажи в Log

### Telegram

- `telegram_setConfig(botToken, chatId)` - Сохранение конфигурации бота (в PropertiesService)
- `telegram_getConfig()` - Получение конфигурации бота
- `telegram_sendMessage(message, parseMode, disablePreview)` - Отправка сообщения
  - `disablePreview` (по умолчанию `true`) - отключает превью ссылок для компактности
  - `parseMode` обычно 'HTML' для форматирования
- `telegram_checkDailyPriceTargets()` - Ежедневная проверка цен и отправка отчетов (триггер 13:00)
  - Отправляет 3 сообщения: общий отчет, позиции достигшие цели, позиции с просадкой
  - Сортирует позиции по прибыльности/просадке
- `telegram_sendDailyReport()` - Отправка общего отчета о портфеле
  - Считает данные напрямую из Invest (не зависит от PortfolioStats)
- `telegram_testConnection()` - Тест подключения к боту
- `telegram_testDailyNotifications()` - Ручной тест ежедневных уведомлений

### Menu

- `onOpen()` - Создание меню при открытии таблицы
- `setupAllTriggers()` - Создание всех триггеров (включая Telegram)
- `removeAllTriggers()` - Удаление всех триггеров
- `initializeAllTables()` - Инициализация всех таблиц (форматирование)
- `syncAnalyticsForInvestSales_()` - Ручная синхронизация аналитики Invest/Sales

### EventHandler

- `onEdit(e)` - Обработка изменений в листах (чекбоксы "Купить?" и "Продать?")
- Обработка чекбокса "Купить?" в History: создание/обновление позиции в Invest
- Обработка чекбокса "Продать?" в Invest: обновление количества или удаление строки, создание/обновление записи в Sales

---

## КОНСТАНТЫ И КОНФИГУРАЦИЯ

### Основные константы (Constants.gs)

```javascript
STEAM_APP_ID = 570  // Dota 2
STEAM_FEE = 0.85    // Комиссия Steam 15%

DATA_START_ROW = 2
HEADER_ROW = 1
ROW_HEIGHT = 85
HEADER_HEIGHT = 35

NUMBER_FORMATS = {
  CURRENCY: '#,##0.00 ₽',
  PERCENT: '0.00%',
  INTEGER: '0'
}

COLORS = {
  PROFIT: '#c8e6c9',      // Светло-зеленый
  LOSS: '#ffcdd2',        // Светло-красный
  STABLE: '#fff9c4',      // Желтый
  BACKGROUND: '#f3f3f3'   // Серый для заголовков
}

UPDATE_INTERVALS = {
  PRICES_MINUTES: 10,     // Каждые 10 минут продолжение сбора
  MORNING_HOUR: 0,        // 00:00 (начало периода "ночь")
  EVENING_HOUR: 12        // 12:00 (начало периода "день")
}

PRICE_COLLECTION_PERIODS = {
  MORNING: 'morning',     // Период "ночь" (00:10-12:00)
  EVENING: 'evening'      // Период "день" (12:00-00:10)
}
```

### Конфигурация анализа трендов

```javascript
TREND_ANALYSIS_CONFIG = {
  SIMPLE_COMPARISON: {
    BASE_THRESHOLD: 0.08,      // 8%
    VOLATILITY_MULTIPLIER: 1.5,
    SIDEWAYS_FACTOR: 0.4
  },
  MOVING_AVERAGES: {
    SHORT_WINDOW: 3,
    LONG_WINDOW: 7,
    BASE_THRESHOLD: 0.02        // 2%
  },
  LINEAR_REGRESSION: {
    WINDOW: 20,
    GROWTH_THRESHOLD: 0.03,     // 3%
    FALL_THRESHOLD: -0.03        // -3%
  },
  MOMENTUM: {
    WINDOW: 5,
    BASE_THRESHOLD: 0.05         // 5%
  },
  CYCLE_PHASE: {
    BOTTOM_THRESHOLD: 0.25,      // 25%
    TOP_THRESHOLD: 0.75          // 75%
  },
  GROWTH_POTENTIAL: {
    PERCENTILE_TARGET: 0.85      // 85-й перцентиль
  }
}
```

### Структура колонок History

```javascript
HISTORY_COLUMNS = {
  IMAGE: 'A',
  NAME: 'B',
  STATUS: 'C',
  LINK: 'D',
  BUY_CHECKBOX: 'E',
  CURRENT_PRICE: 'F',
  MIN: 'G',
  MAX: 'H',
  TREND: 'I',
  PHASE: 'J',
  POTENTIAL: 'K',
  RECOMMENDATION: 'L',
  FIRST_DATE_COL: 14  // Колонка N (M = 13)
}
```

---

## РАСЧЕТЫ И ФОРМУЛЫ

### Invest - Расчет прибыли

```javascript
totalInvestment = quantity * buyPrice
currentValue = quantity * currentPrice
currentValueAfterFee = currentValue * STEAM_FEE  // × 0.85
profit = currentValueAfterFee - totalInvestment
profitAfterFee = (currentValueAfterFee / totalInvestment) - 1
```

### Invest - Усреднение цены покупки

При добавлении позиции к существующей:
```javascript
newQty = currentQty + qtyToAdd
newAvg = (currentQty * currentBuy + qtyToAdd * buyPricePerUnit) / newQty
```

### Sales - Расчет просадки цены

```javascript
priceDrop = (sellPrice - currentPrice) / sellPrice
```
- Положительное значение = цена выросла (хорошо)
- Отрицательное значение = цена упала (плохо)

### PortfolioStats - Метрики портфеля

```javascript
totalInvestment = сумма всех totalInvestment из Invest
totalCurrentValue = сумма всех currentValueAfterFee из Invest
totalProfit = totalCurrentValue - totalInvestment
totalProfitPercent = (totalCurrentValue / totalInvestment) - 1
avgProfitability = средняя прибыльность всех позиций (profitAfterFee)
```

---

## ЧЕКЛИСТ ПРОВЕРКИ ПРИ ИЗМЕНЕНИЯХ

При изменении кода проверять:

- [ ] Группировка цен по дате используется везде, где нужно (тренды, фазы, потенциал)
- [ ] Потенциал хранится как число, не строка
- [ ] Порядок форматирования: сначала условное (`history_applyAllConditionalFormatting_`), потом прямое (`history_highlightMinMax_`)
- [ ] Batch-операции используются для оптимизации (чтение/запись группами)
- [ ] Блокировки освобождаются в `finally` блоке
- [ ] Обработка граничных случаев (null, 0, отрицательные, `Number.isFinite()`)
- [ ] Синхронизация данных между листами работает (History → Invest/Sales)
- [ ] Новые записи вставляются сверху (используется `insertRowAfter(HEADER_ROW)`)

---

## ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: Ошибки при создании колонок в 00:00

**Симптомы**: Ошибки в логах в 00:07, когда триггер пытается обновить цены, но колонка еще не создана

**Решение**: 
- Разрешено создание колонки "ночь" с 00:00 (вместо строго 00:10)
- `history_updatePricesForPeriod` возвращает `false` вместо выброса ошибки
- `unified_priceUpdate` корректно обрабатывает `false`

**Файлы**: `History.gs`, `Utils.gs`

---

## МЕНЮ И ТРИГГЕРЫ

### Главное меню SteamTable

- Включить/Выключить автообновление
- Инициализировать все таблицы
- Обновить цены History (ручное)

### Подменю Invest
- Форматирование
- Изображение и ссылки
- Поиск дублей

### Подменю Sales
- Форматирование
- Изображение и ссылки
- Поиск дублей

### Подменю History
- Форматирование
- Обновить аналитику
- Изображения и ссылки
- Дубли названий
- Создать столбец текущего периода

### Подменю PortfolioStats
- Форматирование
- Сохранить строку истории (тест)

### Подменю Telegram
- Настроить Telegram
- Тест Telegram
- Тест ежедневных уведомлений

### Общие функции
- Синхронизировать цены из History
- Обновить аналитику Invest/Sales
