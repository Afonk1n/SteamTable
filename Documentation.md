# ДОКУМЕНТАЦИЯ ПРОЕКТА SteamTable

**Последнее обновление**: 2025-01-XX  
**Версия**: 1.0

---

## СОДЕРЖАНИЕ

1. [Структура проекта](#структура-проекта)
2. [Критические исправления](#критические-исправления)
3. [Важные моменты по компонентам](#важные-моменты-по-компонентам)
4. [Логика работы](#логика-работы)
5. [Известные проблемы и решения](#известные-проблемы-и-решения)
6. [Рекомендации](#рекомендации)

---

## СТРУКТУРА ПРОЕКТА

### Файлы кода:

1. **Constants.gs** - Константы и конфигурация
2. **SheetService.gs** - Работа с листами
3. **History.gs** - Логика History (основной лист с данными)
4. **Invest.gs** - Логика Invest (портфель инвестиций)
5. **Sales.gs** - Логика Sales (проданные предметы)
6. **Utils.gs** - Утилиты и общие функции
7. **Menu.gs** - Меню и триггеры
8. **EventHandler.gs** - Обработка событий (чекбоксы)
9. **FormattingService.gs** - Форматирование
10. **Calculations.gs** - Расчеты прибыли/убытка
11. **PortfolioStats.gs** - Аналитика портфеля инвестиций
12. **Telegram.gs** - Интеграция с Telegram Bot API для уведомлений

### Листы таблицы:

- **History** - Основной лист с историей цен
- **Invest** - Портфель инвестиций
- **Sales** - Проданные предметы
- **PortfolioStats** - Статистика и аналитика портфеля
- **AutoLog** - Лог автоматических действий
- **Log** - Лог операций покупки/продажи

---

## КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ

### 🔴 ИСПРАВЛЕНО: Несоответствие группировки цен в `history_updateTrends()`

**Файл**: `History.gs`, строки 959-1010  
**Дата исправления**: 2025-01-XX

**Проблема**:
- В `history_analyzeTrend()` цены группировались по дате (ночь/день объединялись)
- В `history_updateTrends()` для расширенного анализа (фаза, потенциал) цены собирались **БЕЗ группировки**
- Результат: тренд рассчитывался на 36 дней, а фаза/потенциал на 72 цены

**Решение**:
Добавлена та же логика группировки по дате в `history_updateTrends()`. Теперь используется одна цена на день (последняя цена за день).

**Важно**: После этого исправления необходимо пересчитать статистику в History через меню.

---

### ✅ ВЫЯСНЕНО: Почему везде было 24 дня

**Файл**: `History.gs`, функция `history_calculateDaysChange_()`

**Причина**:
- ДО исправления: когда тренд не менялся, возвращалось `prices.length` вместо разницы дат
- Если было 24 дня данных → все строки показывали 24

**Решение**: Исправлено ранее - теперь вычисляется реальная разница между датами.

---

### 🔴 ИСПРАВЛЕНО: Ошибки "Ошибка обновления" в логах в 00:07

**Файлы**: `History.gs`, `Utils.gs`  
**Дата исправления**: 2025-01-XX

**Проблема**:
- Триггер настроен на 00:00 (MORNING_HOUR = 0), но проверка времени требовала 00:10
- Когда триггер срабатывал в 00:00, функция `history_ensurePeriodColumn` выбрасывала ошибку
- В 00:07 триггер `unified_priceUpdate` пытался обновить цены, но колонка еще не была создана
- Результат: множество ошибок "Ошибка обновления" в логах каждый день в 00:07

**Решение**:
1. **Изменена проверка времени** в `history_ensurePeriodColumn`:
   - Разрешаем создание колонки "ночь" с 00:00 до 12:00 (вместо строго 00:10)
   - Это предотвращает ошибки, когда триггер срабатывает в 00:00

2. **Улучшена обработка ошибок** в `history_updatePricesForPeriod`:
   - Если колонка не может быть создана (например, из-за проверки времени), функция возвращает `false` вместо выброса ошибки
   - Это нормальная ситуация для `unified_priceUpdate`, который может сработать до создания колонки

3. **Улучшена логика** в `unified_priceUpdate`:
   - Если `history_updatePricesForPeriod` вернула `false`, функция просто выходит без логирования ошибки
   - Это предотвращает ложные ошибки в логах

**Влияние**: 
- ✅ Устранены ложные ошибки в логах
- ✅ Система работает корректно даже если триггер срабатывает до точного времени создания колонки
- ✅ Улучшена надежность автоматического обновления

---

## ВАЖНЫЕ МОМЕНТЫ ПО КОМПОНЕНТАМ

### History.gs - Основной лист

#### Группировка цен по дате
- **ВАЖНО**: Цены группируются по дате, игнорируя период (ночь/день)
- Для каждой даты берется **последняя цена** за день
- Это обеспечивает консистентность между трендом и расширенной аналитикой

#### Расчет трендов
Используется 4 метода анализа с голосованием:
1. Простое сравнение (последние 3 значения)
2. Скользящие средние (короткая 3, длинная 7)
3. Линейная регрессия (окно 20)
4. Momentum анализ (окно 5)

Приоритет при равенстве: 🟥 > 🟩 > 🟨 > 🟪

#### Расчет дней смены тренда
- Ищется последняя смена тренда, начиная с предпоследнего дня
- Если тренд не менялся: разница между первой и последней датой
- Используются реальные даты, а не количество цен

#### Расширенная аналитика
- **Фаза цикла**: Определяется по позиции в диапазоне min-max (25%/75%)
- **Потенциал роста**: 85-й перцентиль цен как целевая цена
- **Рекомендации**: Генерируются на основе фазы, тренда, потенциала и дней смены

#### Порядок применения форматирования
**ВАЖНО**: Сначала условное форматирование, затем выделение min/max
```javascript
history_applyAllConditionalFormatting_(sheet)  // Сначала
history_highlightMinMax_(sheet)                 // Потом
```

---

### Invest.gs - Портфель инвестиций

#### Расчеты прибыли
- `totalInvestment = quantity * buyPrice`
- `currentValue = quantity * currentPrice`
- `currentValueAfterFee = currentValue * 0.85` (комиссия Steam 15%)
- `profit = currentValueAfterFee - totalInvestment`
- `profitPercent = (currentValue / totalInvestment) - 1` (без комиссии)
- `profitAfterFee = (currentValueAfterFee / totalInvestment) - 1` (с комиссией)

#### Усреднение цены покупки
При добавлении позиции к существующей:
```javascript
newAvg = (currentQty * currentBuy + qtyToAdd * buyPricePerUnit) / newQty
```

#### Форматирование колонки "Цель"
- Красный: текущая цена <= 50% цели
- Зеленый: текущая цена >= 80% цели
- Белый: промежуточные значения

---

### Sales.gs - Проданные предметы

#### Расчет просадки цены
```javascript
priceDrop = (sellPrice - currentPrice) / sellPrice
```
- Положительное значение = цена выросла (хорошо)
- Отрицательное значение = цена упала (плохо)

#### Обновление максимальной цены продажи
При повторной продаже используется максимальная цена продажи.

---

### Calculations.gs - Расчеты

#### Batch-операции
Все расчеты выполняются batch-операциями для оптимизации:
- Чтение всех данных одним запросом
- Обработка в памяти
- Запись всех результатов одним запросом

#### Обработка граничных случаев
- Проверка на `Number.isFinite()`
- Проверка на `> 0`
- Обработка `null` значений

---

### Utils.gs - Утилиты

#### Блокировки
Используется система блокировок через `PropertiesService` для предотвращения параллельного выполнения:
- Таймаут: 5 минут (300 секунд)
- Автоматическое освобождение при истечении таймаута

#### Логирование
- **AutoLog**: Автоматические действия (вставка сверху)
- **Log**: Операции покупки/продажи (вставка сверху)

#### Группировка цен по дате
**Критически важно**: Все функции, работающие с ценами, должны использовать группировку по дате:
1. Собрать все цены с их датами
2. Отсортировать по позиции колонки
3. Группировать по дате (ключ: `dd.MM.yy`), беря последнюю цену
4. Отсортировать даты в хронологическом порядке

---

### Menu.gs - Триггеры

#### Архитектура триггеров
1. **00:10** - Создание колонки периода "ночь" и начало сбора
2. **12:00** - Создание колонки периода "день" и начало сбора
3. **Каждые 10 минут** - Продолжение сбора до завершения периода
4. **13:00** - Ежедневные уведомления Telegram (если настроен)

#### Периоды сбора
- **"ночь"**: 00:10 - 12:00
- **"день"**: 12:00 - 00:10 следующего дня

#### Управление триггерами
- `setupAllTriggers()` - Создает все триггеры (включая Telegram)
- `removeAllTriggers()` - Удаляет все триггеры (включая Telegram)
- Триггер Telegram создается только если Telegram настроен

---

### EventHandler.gs - Обработка событий

#### Чекбокс "Купить?" (History)
- Запрашивает количество и цену покупки
- Создает/обновляет позицию в Invest
- Синхронизирует аналитику из History

#### Чекбокс "Продать?" (Invest)
- Запрашивает количество и цену продажи
- Обновляет количество в Invest или удаляет строку
- Создает/обновляет запись в Sales

---

### PortfolioStats.gs - Трекинг динамики портфеля

#### Назначение
- Отслеживание изменений портфеля инвестиций во времени
- Сохранение истории общих показателей портфеля для анализа динамики
- Позволяет видеть как меняется портфель день за днем

#### Структура листа
- **Заголовок** (строка 1): Дата/Время | Вложения | Текущая стоимость | Прибыль/убыток | Прибыль % | Средняя прибыльность
- **Данные истории**: новые записи вставляются сверху (после заголовка, строка 2)
- Используется стандартное форматирование как в других листах (замороженный заголовок, цветовая индикация)

#### Автоматическое сохранение
- **Триггер**: Автоматически сохраняется 1 раз в сутки после завершения дневного сбора цен
- Вызывается в `unified_priceUpdate()` после завершения периода "день" (`PRICE_COLLECTION_PERIODS.EVENING`)
- Функция: `portfolioStats_saveHistory_()` - вставляет новую строку с текущими метриками

#### Метрики (5 базовых)
1. **Общая сумма вложений** - сумма всех вложений в активные позиции
2. **Текущая стоимость** - сумма текущей стоимости всех позиций (с учетом комиссии)
3. **Прибыль/убыток** - общая прибыль/убыток в рублях
4. **Прибыль/убыток (%)** - процент прибыли от общей суммы вложений
5. **Средняя прибыльность** - средняя прибыльность всех позиций в процентах

#### Форматирование
- Ручное форматирование: SteamTable → PortfolioStats → Форматирование
- Автоматическое форматирование при инициализации всех таблиц
- Цветовая индикация: зеленый фон для прибыли, красный для убытков

---

### Telegram.gs - Интеграция с Telegram

#### Конфигурация
- Токен бота и Chat ID хранятся в `PropertiesService` для безопасности
- Настройка через меню: SteamTable → Telegram → Настроить Telegram
- Функции: `telegram_setConfig()`, `telegram_getConfig()`

#### Ежедневные уведомления (`telegram_checkDailyPriceTargets`)
- **Триггер**: Автоматически срабатывает в 13:00 каждый день
- **Отправляет 3 сообщения:**
  1. **Общий отчет о портфеле** - общие вложения, текущая стоимость, прибыль/убыток, количество позиций
     - Данные считаются напрямую из Invest (не зависит от PortfolioStats)
  2. **Позиции, достигшие цели** - все позиции где `currentPrice >= goal` (отсортированы по прибыльности)
  3. **Позиции с сильной просадкой** - все позиции где `currentPrice <= goal * 0.5` (отсортированы по просадке)

#### Формат сообщений
- Используется HTML разметка (`parseMode = 'HTML'`)
- **Названия предметов** содержат гиперссылки на Steam Market в формате `<a href="url">название</a>`
- Заголовки сообщений упрощены (без текста в скобках)
- Все позиции собираются в одно сообщение (не по одной)
- **Превью ссылок отключено** (`disable_web_page_preview: true`) - ссылки кликабельны, но без превью страницы

#### Сортировка позиций
- **Позиции, достигшие цели**: сортируются от самой прибыльной (по проценту прибыли) к менее прибыльной
- **Позиции с сильной просадкой**: сортируются от самых просевших (по проценту просадки) к менее просевшим
- Это помогает сразу видеть наиболее важные позиции в начале списка

#### Улучшенная обработка ошибок
- Добавлено подробное логирование для диагностики проблем
- Корректная обработка исключений с пробрасыванием для логирования
- Функция продолжает работу даже при ошибках отправки отдельных сообщений
- Ежедневный отчет не зависит от PortfolioStats - считает данные напрямую из Invest

#### Тестирование
- Ручной тест: SteamTable → Telegram → Тест ежедневных уведомлений
- Тест подключения: SteamTable → Telegram → Тест Telegram

---

## ЛОГИКА РАБОТЫ

### Поток данных

```
Steam API → History (цены)
    ↓
History → Invest/Sales (синхронизация цен)
    ↓
History → Invest/Sales (синхронизация аналитики: Min/Max, Тренд, Фаза, Потенциал)
```

### Обновление статистики

1. **Автоматическое** (при завершении периода):
   - Обновление цен в History
   - Расчет трендов и дней смены
   - Расчет фазы, потенциала, рекомендаций
   - Выделение min/max
   - Синхронизация с Invest/Sales

2. **Ручное** (через меню):
   - `History → Обновить аналитику`
   - `Обновить аналитику Invest/Sales`

### Синхронизация данных

Все данные синхронизируются из History:
- **Min/Max**: Из всех цен в колонках дат
- **Тренд/Дни смены**: Из колонок I и J
- **Фаза/Потенциал/Рекомендация**: Из колонок K, L, M

---

## ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### ✅ ИСПРАВЛЕНО: Потенциал невозможно сортировать

**Проблема**: Хранился как строка "+14%"

**Решение**: Хранится как число (0.14), форматирование `'+0%;-0%;"—"'` для отображения

**Файлы**: `History.gs`, `Invest.gs`, `Sales.gs`, `Utils.gs`

---

### ✅ ИСПРАВЛЕНО: Выделение min/max перезаписывалось

**Проблема**: Условное форматирование применялось после выделения min/max

**Решение**: Изменен порядок - сначала условное форматирование, потом выделение

**Файл**: `History.gs`, функция `history_updateAllAnalytics_()`

---

### ✅ ИСПРАВЛЕНО: Логи добавлялись вниз

**Проблема**: Новые записи добавлялись в конец, требовался скролл

**Решение**: Использование `insertRowAfter(HEADER_ROW)` для вставки сверху

**Файлы**: `Utils.gs`, функции `logAutoAction_()` и `logOperation_()`

---

### ✅ ИСПРАВЛЕНО: Дни смены учитывали ночь/день как отдельные дни

**Проблема**: "03.11.25 ночь" и "03.11.25 день" считались как 2 дня

**Решение**: Группировка по дате перед расчетом

**Файл**: `History.gs`, функция `history_analyzeTrend()`

---

### ✅ ИСПРАВЛЕНО: Все показывали 24 дня

**Проблема**: Возвращалось `prices.length` вместо разницы дат

**Решение**: Вычисление реальной разницы между датами

**Файл**: `History.gs`, функция `history_calculateDaysChange_()`

---

### ✅ ИСПРАВЛЕНО: Ошибки в логах в 00:07

**Проблема**: 
- Триггер настроен на 00:00, но проверка времени требовала 00:10
- В 00:00 триггер пытался создать колонку, но получал ошибку
- В 00:07 `unified_priceUpdate` пытался обновить цены, но колонка еще не была создана
- Результат: множество ошибок "Ошибка обновления" в логах

**Решение**: 
- Разрешено создание колонки "ночь" с 00:00 (вместо строго 00:10)
- Улучшена обработка ошибок: `history_updatePricesForPeriod` возвращает `false` вместо выброса ошибки
- `unified_priceUpdate` корректно обрабатывает ситуацию, когда колонка еще не создана

**Файлы**: `History.gs`, `Utils.gs`

---

## РЕКОМЕНДАЦИИ

### После исправлений

1. **Пересчитать статистику в History**
   - Меню: `History → Обновить аналитику`
   - Это обновит тренды, дни смены, фазу, потенциал с правильной группировкой

2. **Проверить синхронизацию**
   - Убедиться, что Invest/Sales синхронизируются из History
   - Меню: `Обновить аналитику Invest/Sales`

### Поддержка кода

1. **Группировка цен по дате**
   - Всегда использовать группировку при работе с ценами
   - Не смешивать логику с группировкой и без

2. **Порядок форматирования**
   - Сначала условное форматирование
   - Потом прямое форматирование (выделение min/max)

3. **Хранение данных**
   - Потенциал: число (0.14), не строка ("+14%")
   - Это позволяет сортировку и правильные расчеты

### Производительность

1. **Batch-операции**
   - Использовать batch-чтение и batch-запись
   - Минимизировать количество обращений к таблице

2. **Блокировки**
   - Всегда освобождать блокировки в `finally`
   - Проверять таймаут перед выполнением длительных операций

---

## КОНСТАНТЫ И КОНФИГУРАЦИЯ

### Важные константы

```javascript
STEAM_FEE = 0.85  // Комиссия Steam 15%
DATA_START_ROW = 2
HEADER_ROW = 1
HISTORY_COLUMNS.FIRST_DATE_COL = 14  // Колонка N
```

### Конфигурация анализа трендов

```javascript
TREND_ANALYSIS_CONFIG = {
  SIMPLE_COMPARISON: {
    BASE_THRESHOLD: 0.08,  // 8%
    VOLATILITY_MULTIPLIER: 1.5,
    SIDEWAYS_FACTOR: 0.4
  },
  MOVING_AVERAGES: {
    SHORT_WINDOW: 3,
    LONG_WINDOW: 7,
    BASE_THRESHOLD: 0.02  // 2%
  },
  LINEAR_REGRESSION: {
    WINDOW: 20,
    GROWTH_THRESHOLD: 0.03,  // 3%
    FALL_THRESHOLD: -0.03     // -3%
  },
  MOMENTUM: {
    WINDOW: 5,
    BASE_THRESHOLD: 0.05  // 5%
  },
  CYCLE_PHASE: {
    BOTTOM_THRESHOLD: 0.25,  // 25%
    TOP_THRESHOLD: 0.75       // 75%
  },
  GROWTH_POTENTIAL: {
    PERCENTILE_TARGET: 0.85  // 85-й перцентиль
  }
}
```

---

## СТРУКТУРА КОЛОНОК

### History
- A: Изображение
- B: Название
- C: Статус
- D: Ссылка
- E: Купить
- F: Текущая цена
- G: Min
- H: Max
- I: Тренд - **объединенный формат** ("🟥 Падает 35 дн." или "🟩 Растет 12 дн.")
- J: Фаза
- K: Потенциал (P85) - **число** (0.14 для +14%)
- L: Рекомендация
- M+: Даты (начиная с колонки 13)

### Invest
- A: Изображение
- B: Название
- C: Количество
- D: Цена покупки
- E: Текущая цена
- F: Цель
- G: Общие вложения
- H: Текущая стоимость с комиссией (удалена H - без комиссии)
- I: Профит (удалена K - Прибыль % без комиссии)
- J: Прибыль % с комиссией
- K: Ссылка
- L: Min
- M: Max
- N: Тренд - **объединенный формат** ("🟥 Падает 35 дн.")
- O: Фаза
- P: Потенциал (P85) - **число**
- Q: Рекомендация
- R: Продать

### Sales
- A: Изображение
- B: Название
- C: Цена продажи
- D: Текущая цена
- E: Процент просадки
- F: Ссылка
- G: Min
- H: Max
- I: Тренд - **объединенный формат** ("🟥 Падает 35 дн.")
- J: Фаза
- K: Потенциал (P85) - **число**
- L: Рекомендация

### PortfolioStats
- A: Дата/Время
- B: Вложения
- C: Текущая стоимость
- D: Прибыль/убыток
- E: Прибыль/убыток (%)
- F: Средняя прибыльность

**Примечание:** Лист используется для трекинга истории портфеля. Новые записи вставляются сверху (после заголовка). История сохраняется автоматически 1 раз в сутки после дневного сбора цен.

---

## ВАЖНЫЕ ФУНКЦИИ

### History

- `history_analyzeTrend(row)` - Анализ тренда для строки (с группировкой по дате)
- `history_updateTrends()` - Обновление трендов для всех строк
- `history_updateAllAnalytics_()` - Обновление всей аналитики
- `history_calculateDaysChange_(prices, dates, currentTrend)` - Расчет дней смены
- `history_highlightMinMax_(sheet)` - Выделение min/max ячеек

### Invest/Sales

- `invest_calculateBatch_(sheet, currentPrices)` - Batch-расчет прибыли
- `sales_calculateBatch_(sheet, currentPrices, sellPrices)` - Batch-расчет просадки
- `invest_syncMinMaxFromHistory()` - Синхронизация Min/Max
- `invest_syncTrendDaysFromHistory()` - Синхронизация Тренд/Дней
- `invest_syncExtendedAnalyticsFromHistory()` - Синхронизация Фаза/Потенциал/Рекомендация

### PortfolioStats

- `portfolioStats_calculateMetrics_()` - Расчет текущих метрик портфеля (5 базовых метрик)
- `portfolioStats_formatTable()` - Форматирование листа с заголовками и стандартным форматированием
- `portfolioStats_saveHistory_()` - Сохранение текущих метрик в историю (вставка строки сверху после заголовка)
- `portfolioStats_update()` - Обновление листа (вызывает форматирование)

### Telegram

- `telegram_setConfig(botToken, chatId)` - Сохранение конфигурации бота
- `telegram_getConfig()` - Получение конфигурации бота
- `telegram_sendMessage(message, parseMode, disablePreview)` - Отправка сообщения в Telegram
  - `disablePreview` (по умолчанию `true`) - отключает превью ссылок для компактности
- `telegram_checkDailyPriceTargets()` - Ежедневная проверка цен и отправка отчетов (триггер 13:00)
  - Сортирует позиции по прибыльности/просадке перед отправкой
- `telegram_sendDailyReport()` - Отправка общего отчета о портфеле
  - Считает данные напрямую из Invest (не зависит от PortfolioStats)
- `telegram_testConnection()` - Тест подключения к боту
- `telegram_testDailyNotifications()` - Ручной тест ежедневных уведомлений

### Utils

- `getHistoryPriceForPeriod_(historySheet, itemName, period)` - Получение цены из History
- `syncPricesFromHistoryToInvestAndSales()` - Синхронизация цен
- `acquireLock_(lockKey, timeoutSeconds)` - Блокировка
- `releaseLock_(lockKey)` - Освобождение блокировки
- `getCurrentPricePeriod()` - Определение текущего периода

---

## ЧЕКЛИСТ ПРОВЕРКИ ПРИ ИЗМЕНЕНИЯХ

При изменении кода проверять:

- [ ] Группировка цен по дате используется везде, где нужно
- [ ] Потенциал хранится как число, не строка
- [ ] Порядок форматирования: сначала условное, потом прямое
- [ ] Batch-операции используются для оптимизации
- [ ] Блокировки освобождаются в `finally`
- [ ] Обработка граничных случаев (null, 0, отрицательные)
- [ ] Синхронизация данных между листами работает

---

## ИСТОРИЯ ИЗМЕНЕНИЙ

### 2025-01-XX - Улучшения Telegram уведомлений
- ✅ Убраны тексты "(готовы к продаже)" и "(сигнал покупки)" из заголовков сообщений для более чистого формата
- ✅ Добавлены гиперссылки на предметы в сообщениях Telegram - названия теперь кликабельны и ведут на страницу предмета в Steam Market
- ✅ Отключен превью ссылок (`disable_web_page_preview: true`) - сообщения стали компактнее, без больших превью картинок
- ✅ Добавлена сортировка позиций в сообщениях:
  - Позиции, достигшие цели: от самых прибыльных (по проценту) к менее прибыльным
  - Позиции с просадкой: от самых просевших к менее просевшим
- ✅ Переписана функция `telegram_sendDailyReport()`:
  - Убрана зависимость от PortfolioStats
  - Данные считаются напрямую из Invest
  - Надежнее работает даже если PortfolioStats не настроен
- ✅ Улучшена обработка ошибок при отправке ежедневного отчета:
  - Добавлено подробное логирование для диагностики проблем
  - Улучшена обработка исключений с корректным логированием ошибок
- Формат ссылок: `https://steamcommunity.com/market/listings/{STEAM_APP_ID}/{itemName}`

### 2025-01-XX - Интеграция с Telegram ботом
- Создан файл `Telegram.gs` с функциями для работы с Telegram Bot API
- Реализованы ежедневные уведомления в 13:00 (отдельный триггер)
- Реализован ежедневный отчет о портфеле (общие показатели)
- Реализованы отчеты о позициях, достигших цели и позициях с сильной просадкой
- Токен и Chat ID хранятся в `PropertiesService` для безопасности
- Триггер Telegram привязан к включению/отключению автообновления
- Добавлены функции тестирования в меню Telegram

**Использование:**
- Настройка: SteamTable → Telegram → Настроить Telegram
- Тест подключения: SteamTable → Telegram → Тест Telegram
- Тест уведомлений: SteamTable → Telegram → Тест ежедневных уведомлений
- Уведомления отправляются автоматически в 13:00 каждый день

### 2025-01-XX - Рефакторинг PortfolioStats - Трекинг динамики портфеля
- ✅ Полностью переписан модуль PortfolioStats для трекинга истории портфеля
- ✅ Упрощена структура: убраны топ-5 прибыльных/убыточных, распределения по фазам/трендам, диаграммы
- ✅ Оставлены только 5 базовых метрик: вложения, текущая стоимость, прибыль/убыток, прибыль %, средняя прибыльность
- ✅ Реализован автоматический трекинг: сохранение метрик в историю 1 раз в сутки после дневного сбора цен
- ✅ Новые записи вставляются сверху (после заголовка) для удобного просмотра последних данных
- ✅ Стандартное форматирование как в других листах (замороженный заголовок, цветовая индикация)
- ✅ Добавлено форматирование в подменю PortfolioStats → "Форматирование"
- ✅ Интегрировано в `initializeAllTables()` для автоматической инициализации
- ✅ История сохраняется автоматически в `unified_priceUpdate()` после завершения дневного периода

### 2025-01-XX - Добавлена аналитика портфеля (PortfolioStats) - УСТАРЕЛО
- ~~Создан новый лист "PortfolioStats" с автоматическим расчетом статистики портфеля~~
- ~~Реализованы функции расчета метрик: общие показатели, топ-5 прибыльных/убыточных, распределение по фазам и трендам~~
- **Примечание:** Модуль полностью переписан (см. выше "Рефакторинг PortfolioStats")

### 2025-01-XX - Критическое исправление группировки цен
- Исправлена группировка цен в `history_updateTrends()`
- Теперь фаза и потенциал используют ту же логику, что и тренд

### Ранее исправлено:
- Потенциал: хранение как число для сортировки
- Выделение min/max: исправлен порядок форматирования
- Логи: вставка сверху вместо добавления вниз
- Дни смены: группировка по дате и расчет разницы дат

---

**Документация актуальна на**: 2025-01-XX

